---
title: "MILOS"
type: entity
created: 2026-04-09
updated: 2026-07-26
tags: [product, spa, react, islamic, operating-system, seven-maqasid]
sources: 0
---

# MILOS

MILOS V2.1 is an Islamic life operating system built as a React 19 single-page application with Zustand 5 state management, React Router 7 navigation, and Vite 8 bundling. It manages work, money, people, and operations across Eight Maqasid-derived pillars — Faith, Health, Intellect, Family, Wealth, Environment, Community, and Moontrance — providing module dashboards with a three-tier detail design, [[bbos-pipeline]] integration, and Islamic UI components such as CeremonyGate, ReadinessCheck, and PrayerOverlay. The project uses a CONTEXT.md routing hierarchy for developer orientation and is deployed via GitHub Pages. The monorepo now includes `website/` ([[ogden-hub]] subtree) and `atlas/` ([[olos]] git submodule). Its knowledge graph, built via [[graphify]], contains 1,012 nodes, 1,517 edges, and 126 communities.

## Key Facts

- **Stack:** React 19 + Zustand 5 + React Router 7 + Vite 8 + dnd-kit + Lucide icons + date-fns
- **Architecture:** SPA with module dashboards, three-tier detail design (overview / detail / action)
- **Modules:** Faith, Health, Intellect, Family, Wealth, Environment, Community, Moontrance, plus BBOS, Money, People, CRM (pillar ids in code: `health`, `ummah`)
- **Islamic UI components:** CeremonyGate, ReadinessCheck, PrayerOverlay, ThresholdModal, ResumeOverlay
- **Context system:** CONTEXT.md routing hierarchy — root routes to domain-level files before source is touched
- **Deployment:** GitHub Pages
- **Monorepo structure:** `src/` (app), `website/` ([[ogden-hub]]), `atlas/` ([[olos]])
- **Knowledge graph:** 1,012 nodes, 1,517 edges, 126 communities via [[graphify]]

## Current Status

Active development on V2.1. Rich subtask descriptions (Why/How format) now complete for 6 of 7 pillars: Faith, Life, Intellect, Family, Wealth (236 subtasks), and Environment (226 subtasks). Only Ummah remains. 35-finding technical audit remediation completed across 8 sprints (2026-04-10 to 2026-04-11). Dashboard refreshed with `PillarProgressStrip` and live Maqasid Focus panel. Module dashboards for all seven pillars implemented. BBOS pipeline integrated. AyahBanner polished. Family pillar Islamic data bootstrapped — `MODULE_ATTRS['family']` now has full dua (Quran 25:74), divine attrs (Al-Wadud, Al-Qayyum), readiness checks, and reflection, unblocking the Opening Threshold for all family sub-modules. Intellect "Professional" sub-module renamed to "Skill Proficiency" across all 5 data/page files. Opening Threshold ceremony reframed 2026-04-21 — no Defer exits anywhere, no Confirm gate, and a bilingual Bismillah / Alhamdulillah finalize button; `faith-salah` readiness now uses naming-style copy (*"What am I bringing…"* / *"I am carrying…"*) as the template for the remaining six pillars. PropheticPath timeline refactored 2026-04-21 — six prayer nodes (Fajr, Dhuhr, Asr, Maghrib, Isha, Tahajjud) now expose phase-filtered **Before / Main / After** task buckets sourced via `prayer-phase:*` tags + per-node regex matchers in `src/data/prophetic-path-submodules.js`; four new prophetic-sunnah parent tasks authored in `faith-seed-tasks.js` (pre-prayer sunnah, post-prayer adhkar, Ishraq remembrance, prophetic supplications) with Amanah-Gate source blocks citing Muslim Scholar NotebookLM (Bukhari, Muslim, Quran). Before button decoupled from Threshold modal; seed backfill extended in `project-store.js` so existing boards pick up new tasks by title-diff without state loss. MirrorCard Education tab rewired 2026-04-21 to enumerate pillar-canonical submodules via `getPillarSubmoduleIds` — clicking a submodule card opens the canonical `PillarLevelPage` as a `SubmoduleSlideUp` overlay (identical to `/app/<submodule>` route). `/app/work` New Project dialog now exposes Pillar + Submodule pickers persisted as `project.moduleId`; `buildUserProjectsForScope` matches both pillar-only and submodule `moduleId` via an explicit `PILLAR_TO_SUBMODULES` table (Ummah uses bare ids — no prefix). `submodule-registry.js` extended with the Ummah pillar + `community → ummah` alias; `TaskDetailPanel` overlay z-index raised 99 → 1100 so task popups render above slide-ups. **Main dashboard reorganized 2026-04-23** into three Islamic tiers — Qalb (نية · Intent) / Amal (عمل · Action) / Barakah (بركة · Impact) — per [[2026-04-23-dashboard-three-tier-redesign]]. Kills: Open Tasks panel, standalone Maqasid Focus, Activity Feed, red Workflow Pressure. Merges: Manifesto + Evening Reflect → `DailyMithaq` covenant widget (Niyyah → Muhasaba → Fulfilled, Maghrib-pivoted via `usePrayerTimes()` with 6pm fallback); stat cards + Today → `Daily Snapshot` 3-metric grid. New: `SakinahMeter` (inverted semantics, never `--danger`) and 7-axis `MaqasidBalanceRadar` (polygon shape over 30-day completions, no numeric scores). BCG Chart relocated from Amal to Barakah. OnboardingChecklist self-dismiss already satisfied via 4-second celebration `useEffect`. **Pre-test Tier-1 audit complete 2026-04-25** — `pillar/*` thick routes now route through `CeremonyGuard` with `*-core` Level-1 semantics + a `CeremonyGuardDynamic` catch-all for unknown ids; `pillar/moontrance` deferred (no top-level `MODULE_ATTRS['moontrance']` yet, blocked on NotebookLM grounding pass per Amanah Gate). Storage and migration silent `catch {}` blocks replaced with `console.warn('[bbiz]…')` and `bbiz:storage-error` CustomEvent dispatch on backup/restore/migration write failures, surfacing through the existing storage-error banner. Onboarding `finish()` now seeds today's niyyah from the user's selections (`completeNiyyah({ pillars, submodule, level: 'core' })`) or `skipNiyyah()` on Skip-for-now, eliminating the duplicate ceremony fire on first dashboard visit. Two audit findings turned out incorrect on inspection (Moontrance pillar-level `MODULE_ATTRS` already present at islamic-data.js:3914/4029/4144; `toastStore.js` is not an orphan — 10 callers for pillar-pulse toasts, distinct from the 11-caller operation-toast `toast-store.js`); both stores now carry cross-referencing docstrings. See [[2026-04-25-milos-pre-test-tier-1-fixes]]. **Pre-test Tier-2 polish complete 2026-04-25** — pillar wisdom + next-actions populated for 23 sub-modules across Life/Intellect/Family/Wealth/Environment/Ummah, all Arabic + English fetched via quran.ai MCP (Amanah Gate honored, Moontrance + Ogden stubs preserved). Prophetic Path graduated from `/app/prophetic-path-test` → `/app/prophetic-path` with redirect, prototype tooltip dropped from Sidebar/MobileNav. New `ChunkErrorBoundary` (class component, `getDerivedStateFromError` + Retry → `window.location.reload()`) wraps the App.jsx global Suspense and TaskDetailPanel SubtaskSources Suspense — lazy-load failures recover instead of spinning forever. PropheticPath now consumes `usePrayerTimes` `loading + error + requestLocation` and renders a degraded banner when timings are null instead of silently rendering empty Before/After lists. Storage `safeSet` adds a `navigator.storage.estimate()` fire-and-forget probe for writes >200 KB, dispatching the existing `bbiz:storage-error` event preemptively at >90% quota. Moontrance Land/Seasonal/Residency converted to lazy routes — index chunk shrank ~322 KB. **B.2 audit correction:** Ummah seed-task citations were claimed missing, but lint-grounding.mjs reports 0 missing across all 525 subtasks (legacy-string Markdown format); the 2026-04-17 deferral was the two-axis migration, not citation authoring. **Caveat:** the LevelNavigator chunk is still 4.7 MB raw — the 3 Moontrance pages were not the source; root cause is shared data hoisting across all 8 pillars and needs a Vite manual-chunks investigation, filed as deferred. See [[2026-04-25-milos-tier-2-polish]]. **Deferred-items closeout complete 2026-04-25** — three items deferred from the pre-test audit ([[2026-04-25-milos-pre-test-audit]]) shipped in a follow-up session. **Phase A** ([[2026-04-25-milos-chunk-split]]): LevelNavigator chunk 4,699 KB → 178 KB raw / 47 KB gz (96% reduction). [src/services/seed-hydrator.js](src/services/seed-hydrator.js) rewritten with lazy `PILLAR_LOADERS` dynamic-import map + idempotent promise cache; sync API preserved (passthrough on cache miss) so only entry points need to await. `loadTasks` and the 8 `ensureXProjects` actions now async with `await preloadPillarSeeds(<pillar>)` first line. `backfillAndStripSeeds()` discovers stored pillars from `localStorage.projects` and only loads those — fresh-install user opening one pillar pays for one chunk, not eight. [vite.config.js](vite.config.js) `manualChunks` produces 9 named pillar-seed chunks. **Phase B** ([[2026-04-25-milos-lint-clean]]): lint backlog 658 problems → 0 errors / 0 warnings. Custom [scripts/strip-template-escapes.mjs](scripts/strip-template-escapes.mjs) state-machine fixes 512 `\'`/`\$` escapes ESLint declined to auto-fix in backtick template literals. 13 new `*-constants.js` sibling files split data exports out of LevelNavigator/CorePage components (kills 21 `react-refresh/only-export-components` errors and unblocks Fast Refresh on pillar pages). 28 React-hooks fixes including a load-bearing rules-of-hooks structural hoist in [src/components/work/ProjectBoard.jsx](src/components/work/ProjectBoard.jsx) (cross-fade hook block lifted above `if (!project) return null;` early return) — was a latent conditional-hook crash in error states. `vite.config.js` migrated to ESM `path.dirname(fileURLToPath(import.meta.url))`. **Phase C.1** ([[2026-04-25-milos-grounding-tooling]]): test framework arrived. Vitest 4.1.5 installed, [src/data/seed-tasks/__tests__/grounding.test.js](src/data/seed-tasks/__tests__/grounding.test.js) (40/40 passing) ratchets per-pillar legacy-string counts so migration progress is monotonic — counts can only decrease. `lint-grounding.mjs --strict` fails on legacy entries; new `npm run lint:grounding-strict` script. Current strict-mode signal: 1,904 legacy + 1 schema error — the load-bearing failure that future migration batches drive to zero. **Phase C.2** ([[2026-04-25-milos-faith-grounding-complete]]): Faith pillar two-axis migration shipped. NotebookLM Muslim Scholar timeouts (2 of 3 probe queries) prompted a pivot to parser-first deterministic migration — [scripts/migrate-faith-grounding.mjs](scripts/migrate-faith-grounding.mjs) state-machine-parses every legacy markdown `sources` template literal (handling escaped backticks via `(?:\\\`|[^`])*`), splits on `### ` headers, normalizes refs (`Quran (X:Y)` → `Quran X:Y`; collection name canonicalization), maps Sahih→Bayyinah / Hasan→Qarina / else→Niyyah, and serializes structured arrays preserving column indent. 212 entries migrated, 0 skipped. 7 entries (Quran 65:3, 98:5, 49:15, 63:1-3, 63:1-4, 5:48, 33:40, 42:11, 45:23) manually patched with authoritative Arabic + Abdel Haleem translation via Quran MCP after parser captured `**II. Hadith**` section header / nothing as translation. Round-trip audit ([scripts/audit-faith-migration.mjs](scripts/audit-faith-migration.mjs)) compared 10 random migrated entries to git-snapshotted legacy: 0 ref-count mismatches, all Arabic/translation snippets present in source. Faith ratchet 212 → 0; strict-mode count 1904 → 1692 legacy. Faith joins Prayer as the second fully two-axis-compliant pillar. Preview verification skipped — `/faith-shahada` CeremonyGuard overlay blocked screenshot tool (innerText empty, 30s timeout); mitigating evidence is the 32 pre-existing Faith structured entries already rendering through `SubtaskSources.jsx:151-202`'s array branch + 40/40 schema test green. **Phase C.3 — Ummah pillar migration shipped 2026-04-25** ([[2026-04-25-milos-ummah-grounding-complete]]). [scripts/migrate-ummah-grounding.mjs](scripts/migrate-ummah-grounding.mjs) is a verbatim copy of the Faith parser with FILE path swap; zero grammar changes needed. Four new collection prefixes (`Jami at-Tirmidhi`, `Musnad Ahmad`, `Sunan Ibn Majah`, `Sunan an-Nasai`) all already canonical sunnah.com form — pass through unchanged. 525 entries migrated; 1 Quran-MCP patch (55:7-9, same `**II. Hadith**` capture failure mode that hit 7 Faith entries). Round-trip audit ([scripts/audit-ummah-migration.mjs](scripts/audit-ummah-migration.mjs)) 10/10 ref-count clean. Ummah ratchet 525 → 0; strict count 1692 → 1167 (delta 525). Preview verification this time *did* succeed — `/app/work/ummah_collective_core/tasks` → "Sustain daily du'a" → "Memorise Quranic and Prophetic du'as" → `Source` panel renders Surah An-Nisa [4:75] structured Quran card with Bayyinah + Direct chips, Arabic word-by-word breakdown with transliteration, quran.com link; hadith cards (Sahih al-Bukhari 2448, Sahih Muslim 1882) display Sahih grade chip and sunnah.com link. Two pillars (Faith + Ummah) now fully migrated; parser confirmed reusable across pillars sharing the legacy convention — the remaining five (Life/Intellect/Family/Wealth/Environment, ~1,167 entries) are one-FILE-swap each. **Phase C.4 — Life pillar migration shipped 2026-04-25** ([[2026-04-25-milos-life-grounding-complete]]). [scripts/migrate-life-grounding.mjs](scripts/migrate-life-grounding.mjs) is a `sed`-generated copy of the Ummah parser. **236 entries migrated, zero schema violations on first run, no Quran-MCP patches needed** — cleanest migration yet, on heterogeneous content (Physical Health, Mental Health, Safety boards). Round-trip audit 10/10 clean. Life ratchet 236 → 0; strict count 1167 → 931 (delta 236). Preview verified — `/app/work/life_physical_core/tasks` → "List all regularly consumed foods" subtask → Source panel renders Surah Al-Baqarah [2:168] structured card with Bayyinah + Direct chips, full Arabic word-by-word breakdown with transliteration, Abdel Haleem translation. Three pillars complete (973 entries); the remaining four (Intellect/Family/Wealth/Environment = 931) can batch in a single follow-up session given the zero-patch trend. **New `/app/orientation` screen shipped 2026-07-23** ([[2026-07-23-milos-orientation-screen]]) — a single-recommendation "what's next" surface replacing backlog-browsing: one tier-ranked subtask at a time with a 5-rung breadcrumb ladder, inline Qur'an/hadith evidence (reuses `SubtaskSources`), and 4 actions (Mark done / Doesn't apply / Not today real-snooze / Something else pillar override). New `src/data/orientation-selector.js` ranks pillars by their own first-incomplete-necessity-tier (Daruriyyat → Hajiyat → Tahsiniyat, never blended); new `src/components/orientation/` component set; discoverable from Sidebar + Dashboard's `TodayFocusSection` handoff row (not MobileNav, by design). `npm test` 94/94, lint clean, build green; screenshot tool unavailable this session ([[project-screenshot-hang]]), verified via DOM/text instead. Uncommitted on `feat/desktop-pillar-glyphs`. **Prayer node popup consolidated 2026-07-25** ([[2026-07-25-milos-prayer-popup-consolidation]]) — operator: *"I only want to see the list of tasks and not the list of prayers"* and *"the during tab should contain the content it currently refers/links to."* On the six prayer nodes, `NodePhaseSlideUp` Before/After now render the **task list alone** (`PrayerSunnahSummary` deleted — the 2026-07-23 Sunnah-tabs rendering is reversed) and During renders `PrayerHeroDuring` **inline** instead of a hand-off button. This orphaned and deleted six files — `PrayerSlideUp` (+CSS), the cloned `PrayerLevelPage` + `PrayerLevelNavigator`, and `PrayerSunnahSummary` (+CSS) — partially superseding the 2026-04-21 FLN slide-up decision (its 18 boards + seed model survive). `PRAYER_GUIDE` / `getPrayerPhaseSunnah` / `SUNNAH_LEAD` are retained but now unrendered (still seed the anatomy boards). Authors no fiqh; graded data stays in-repo. `npm test` 94/94, edited-file ESLint clean, `npm run build` ✓; screenshot tool **worked** this session — During inline guide + Pray-Along verified in the popup. Uncommitted on `feat/desktop-pillar-glyphs`. **Non-prayer node popup consolidated the same day 2026-07-25** ([[2026-07-25-milos-nonprayer-tasks-to-during]]) — the inverse-scope complement (the prayer decision had left non-prayer nodes untouched). Operator: *"Move the task list from before and after to the during tab."* Non-prayer nodes previously split their task pool across the three tabs by keyword `phaseMatchers`; now the `phaseTasks` non-prayer branch calls `buildTasksForNode(node.id, …, { phase: null, limit: 20 })` so **During shows the combined Before+main+After pool**, and **Before/After render `CeremonySummary` alone** (the `.pp-phase-tasks` task-list wrapper + its CSS rule removed). `midday-labor`'s bespoke `showProjects` Projects/Education view was dropped per operator choice — its During now shows the task list like every peer; `showProjects`/`scopeProjects` and the `buildUserProjectsForScope`/`submodulesForNode`/`getPillarSubmoduleIds` imports removed from `NodePhaseSlideUp.jsx`. `npm test` 94/94, edited-file ESLint clean, `npm run build` ✓; the **screenshot tool was unresponsive this session** — DOM-verified (Midday Labor During 20-card list incl. both weekly-planning tasks, no project list; Before/After ceremony-only; Fajr unchanged). Full `npm run lint` still red only on the pre-existing unrelated `Orientation.jsx:42` `Date.now()` error. Amanah neutral. Uncommitted on `feat/desktop-pillar-glyphs`. **All six prayer nodes' During guides completed 2026-07-25** ([[2026-07-25-milos-prayer-during-guides-all-nodes]]) — the third same-day prayer-popup session finished the During tab, which still showed a coming-soon shell on four nodes (only Isha + Fajr had a guide; the shell's stale *"Isha is available today"* copy masked Fajr). A new shared `buildPrayerSequence` builder ([prayer-during-builder.js](src/data/prayer-during-builder.js)) holds recitation objects **copied verbatim** from the verified `isha-during.js` and computes posture counts; four thin `-during.js` files register Dhuhr/Asr (4-rakʿāt silent), Maghrib (3-rakʿāt, aloud r1–2), and Tahajjud (2-rakʿāt **nafl** unit) in `prayer-sequences.js`, with Isha/Fajr left untouched. Because the Reference header hardcodes *"How to pray {label} **Fard**,"* Tahajjud carries an optional `headline` override — **"How to pray Tahajjud — 2 rakʿāt at a time"** — so a voluntary prayer is never labelled Fard. Four provenance-only `nb_salah_{dhuhr,asr,maghrib,tahajjud}.json` corpora reuse Isha's recitation entries byte-identical, citing only in-repo sources. **Amanah positive** — no invented hadith/Arabic, structures confirmed against `PRAYER_GUIDE`. `npm test` 94/94, build ✓ 1.33s, the three documented lint gates green (prayer 87/87 grounded); composite `npm run lint` red only on the pre-existing unrelated `generate:pillar-glyphs:check` transport timeout (a health-pillar file, proven not-mine). Screenshot tool unavailable ([[project-screenshot-hang]]) → DOM-verified all four guides render correctly with zero mojibake; the critical check passed — Tahajjud header carries no "Fard." Uncommitted on `feat/desktop-pillar-glyphs`. **`/app/orientation` reskinned into a carousel of 7 Maqāsid domain cards + bottom sheet 2026-07-25** ([[2026-07-25-milos-orientation-carousel-redesign]]) — a UI reskin over the already-shipped tier-gated engine (not a rebuild), after user testing found the 2026-07-23 ladder + balance-strip surface hard to act on. Four decisions locked via AskUserQuestion (redesign in place / defer streak + "welcome back" banner / extend the engine for priority-aware task selection / mobile-first 520px); scope held to the standalone route (`Dashboard.jsx`/`/app` home untouched). The engine gained `PRIORITY_RANK` (sort by priority then `order`) + `buildOrientationCarousel(...) → {cards, recommendedPillarId}`; `OrientationLadder`/`OrientationBalanceStrip` retired for `OrientationCarousel`/`OrientationCard`/`OrientationSheet` (the sheet reuses the shared `pp-slideup__*` chrome + `useFocusTrap`); `OrientationActions` trimmed 4→3 (the carousel *is* the pillar picker). The rewritten container also **cleared the pre-existing `Date.now()` purity lint error** the prior orientation ADR left red. The tier gate proved live — Health (0/96, weakest active-tier ratio) was recommended over Faith (1/139) by ratio, not by task priority. `npm test` 102/102, build ✓, lint green across all three gates; **the screenshot tool worked this session** (carousel light/dark + open Health sheet captured, token theming flips cleanly, 0 console errors). Surfaced out-of-scope: pre-existing UTF-8/cp1252 **mojibake** in 3 data files (485 occ, incl. `health-readiness-ayat.js` ⚠️ Qur'an data), flagged as `task_2e56b6bf` ([[project-seed-mojibake]]). Uncommitted on `feat/desktop-pillar-glyphs`. **Centered popups + orientation desktop + node-popup drill-in shipped 2026-07-26** ([[2026-07-26-milos-centered-popups-node-drill-in]]) — the clipped "Weakest — recommended" badge fixed (track top padding `--space-1`→`--space-3` against the forced-`overflow-y` clip); the deferred **desktop layout delivered** as a stage + 7-row side rail (new stateless `OrientationSpread.jsx` gated by `useMobile()`; rail click swaps `focusPillarId`, only the stage card opens the sheet; 768–1023px wraps the rail as chips); **all four `pp-slideup__*` surfaces are now centered modals at every viewport** (`ProjectSlideUp.css` is the app-wide chrome — `min(92vw,620px)`×85vh panel, `pp-slideup-pop` entrance, `--wide` variant; the load-bearing `display:contents` portal rule untouched); and the Prophetic Path node popup **dropped its `TaskDetailPanel` hand-off** for an inline Orientation-style drill-in (key-not-snapshot `detailKey`, `findNextEligibleSubtask` Now box, Why & how + Evidence accordions, Mark done / Doesn't apply / Not today hitting the store directly, complete-state with no auto-nav). Step internals extracted to `shared/SubtaskStepDetail.jsx` so sheet + popup render one implementation; `OrientationEvidence`→`shared/SubtaskEvidence.jsx`; `computeTodayKey`→`src/utils/islamic-day-key.js`. `npm test` 102/102, eslint 0, grounding gates green; composite lint red only on the pre-existing environmental `generate:pillar-glyphs:check` SSR RunnerError (retried per operator decision, still failing, unrelated). Screenshot-verified light+dark, mobile+desktop+~900px. Uncommitted on `feat/desktop-pillar-glyphs`. **Seed mojibake repaired + glyph generator fixed 2026-07-26** ([[2026-07-26-milos-mojibake-repair-glyphs-loader-fix]]) — all 485 UTF-8/cp1252 corruption occurrences ([[project-seed-mojibake]] → **Resolved**) across `health-seed-tasks.js` / `health-readiness-ayat.js` (Qur'an data) / `niyyah-feelings.js` restored via `git restore --source=9f87e8d` and **proven** by an independent byte-reversal verifier (strip BOM → cp1252-encode with C1 fallback → byte-identical ×3; zero invented revelation text); niyyah's spurious BOM gone; residual `â` is legitimate Hilali-Khan orthography. The recurring `generate:pillar-glyphs:check` SSR timeout root-caused by probe bisection to **Vite loading `vite.config.js` inside `createServer`** (not the lucide barrel) — the generator's loader now passes `configFile: false` and imports the config directly for its `resolve.alias` only; check ~1.5s deterministic with the dev server running. **Composite `npm run lint` fully green for the first time on this branch** (eslint + grounding-strict + inline-refs + glyphs check); `npm test` 102/102. Both fixes uncommitted — **ayat file queued for operator covenant review before commit**. **Sequential task/subtask locking shipped 2026-07-26** ([[2026-07-26-milos-orientation-sequential-locking]]) — `/app/orientation` now shows a board's whole ordered task/subtask chain (lettered **Task pills** + numbered **Subtask chips**) with only the true next-incomplete step actionable and every later step **locked but previewable**; sequence is **array order** of `tasksByProject[boardId]` (`task.n` is display-only). The engine replaced `findFirstEligibleInPillarTier` with `findActiveBoardInPillarTier` and made `recommendOrientation` fall through **cross-board then cross-pillar** when a pillar's current task is snoozed (necessity-tier triage preserved); the card shape stayed **additive** and the new `OrientationSteppers` render via **defaulted guard props** on the shared `SubtaskStepDetail`, so the Prophetic Path node popup is **byte-identical**. "Not today" now snoozes the whole **task** (no new store action). One behavior change: selection is array-order, not priority-order (seed-order curation deferred). `npm test` 142/142, composite `npm run lint` fully green, `npm run build` ✓ 1.28s; screenshots unavailable ([[project-screenshot-hang]]) → verified via live DOM + `localStorage`. Uncommitted on `feat/desktop-pillar-glyphs`.

**`@ogden/ui-components` adopted 2026-04-30** ([[2026-04-30-ogden-ui-components-github-direct]]) — `LevelNavigator`, `MaqasidComparisonWheel`, `IslamicTerm` and 9 satellites (`WheelWisdomTooltip`, `MaqasidNextActionCard`, `wheelHoverStore`, `mithaqStore`, `useMilestoneWatcher`, `useMithaqHold`, `arabic` utils) extracted to a standalone Vite library at `onaxyzogden/ogden-ui-components` and consumed via GitHub-pin URL (`github:onaxyzogden/ogden-ui-components#v0.1.0`). MILOS adopts via thin wrapper components at the original three paths so ~60 import sites remain unchanged; singleton stores and ritual hooks collapsed to one-line re-exports to keep module-instance identity intact (else hover sync + 1.5s covenant ritual silently desync). `vite.config.js` requires `optimizeDeps.exclude: ['@ogden/ui-components']` to bypass pre-bundling that fragments externalized React → "Invalid hook call". Persist key changed `bbiz_milos-mithaq` → `bbiz_ogden-mithaq` (activations expire at next Fajr, accepted). Atlas + Moontrance adoption deferred to dedicated sessions. 56/56 tests + 3 ratchets at 0; verified live at `/app` (wheel + LevelNavigator hover sync working).

**Online accounts disabled 2026-07-27** ([[2026-07-27-milos-disable-online-accounts]]) — operator: *"the backend for MIOS online signup (not local) account signup/login is not ready so let's remove it from the live web app for now."* The Supabase auth + cloud-sync system shipped in `9cf732a` (2026-06-04) was **live in production** — `maqasid.ogden.ag/auth` served a working signup form, meaning the `VITE_SUPABASE_*` secrets had been added (the wiki's 2026-06-05 log entry still listed that as outstanding; that record was stale). One hardcoded `CLOUD_ACCOUNTS_ENABLED = false` in [supabase.js](src/services/supabase.js) — deliberately not an env var, so a stray `.env` or re-added CI secret cannot silently re-expose signup — plus two `<Navigate>` redirects (`/auth` → `/get-started`, `/app/account` → `/app/settings`, needed because `App.jsx` has no `*` 404 route) and removal of the `deploy.yml` secrets block. 8 files, `+61/-34`. Freezing `authStatus` at `'guest'` is the chokepoint that auto-disables `SyncStatusChip`, `FirstLoginModal`, `useSyncObserver` and Settings' Account & Sync panel **with zero edits to those four files**. The local profile half of `auth-store` (`user` / `bbiz_user`, which `ProtectedRoute` gates all of `/app` on and ~15 components read) is byte-identical — the two identity notions are independent and conflating them would have logged everyone out. Also fixes an **honesty gap**: the FAQ's *"Nothing is sent to external servers"* was false for signed-in users (sync pushed full `localStorage` snapshots to bucket `mios-snapshots`) and is now true again, per the [[2026-06-19-atlas-offline-demo-deploy]] precedent. `@supabase/supabase-js` **fully tree-shaken** — the compile-time `const false` lets Rolldown fold the ternary; 218 KB of JS removed, `GoTrueClient` absent from `dist/`. The ADR also **backfills the missing sync-architecture record** (bucket `mios-snapshots`, table `user_snapshots`, full-snapshot last-write-wins, `SCHEMA_VERSION 5.0`, reload-on-pull) that `9cf732a` claimed existed but never had. ~1,150 lines dormant on disk; restore = flip one boolean + 4 lines. `npm test` 94/94, changed-file ESLint clean, `npm run build` green; screenshot tool unavailable again ([[project-screenshot-hang]]), verified via DOM. Uncommitted on `claude/remove-mios-online-signup-0a709e`.

## Connections

- [[bbos-pipeline]] — Business operating system module integrated into the app
- [[ogden-hub]] — Marketing site merged as subtree at `website/`
- [[olos]] — Land intelligence app linked as git submodule at `atlas/`
- [[graphify]] — Knowledge graph extraction tool used for codebase analysis
- [[maqasid-al-shariah]] — Jurisprudential framework grounding the Seven Maqasid pillar system
- [[amanah-gate]] — Ethical screening pattern used in BBOS qualification stage
- [[ceremony-gate-pattern]] — UI pattern for Islamic ritual transitions (prayer, readiness)
- [[covenant-architecture]] — Structural philosophy governing pipeline and module design

## Open Questions

- Will the three monorepo segments (app, website, atlas) share any dependencies or remain fully isolated?
- What is the deployment strategy for atlas.ogden.ag vs the main GitHub Pages site?
- ~~Should the knowledge graph be regenerated after the audit remediation completes?~~ Done — regenerated 2026-04-11 (1,012 nodes, 126 communities)
- When will the dual contact stores (contacts_v2 / crm_contacts) be unified? (see ADR: [[dual-contact-stores]])

## History

| Date | Event |
|---|---|
| 2026-07-27 | **Online accounts (Supabase signup/login/cloud-sync) disabled behind `CLOUD_ACCOUNTS_ENABLED = false`.** Backend not ready, but the feature was live at `maqasid.ogden.ag/auth`. Hardcoded master switch in `src/services/supabase.js` (not an env var, by design); `/auth` → `/get-started` and `/app/account` → `/app/settings` via `<Navigate replace>` because there is no `*` 404 route; `VITE_SUPABASE_*` env block removed from `deploy.yml` so the anon key stops shipping. Landing copy *"To sync across devices, create a free account"* → *"You can export a full backup any time from Settings"*; Onboarding drops to 3 steps. Local profile (`user` / `bbiz_user`) untouched — `ProtectedRoute` gates `/app` on it, not on Supabase. 8 files `+61/-34`; ~1,150 lines left dormant. Supabase lib fully tree-shaken (−218 KB). Sync architecture backfilled into the ADR. See [[2026-07-27-milos-disable-online-accounts]]. |
| 2026-07-26 | **Sequential task/subtask locking on `/app/orientation` — everything visible, only the true next step actionable, with browse-ahead preview.** The carousel/desktop surface ([[2026-07-25-milos-orientation-carousel-redesign]], [[2026-07-26-milos-centered-popups-node-drill-in]]) surfaced one step per domain and discarded siblings; per an operator prototype (`maqasid-orientation-single.html`) + delta spec (`maqasid-locking-update-brief.md`) it now shows the whole ordered chain and forbids acting out of order. Sequence is **array order** of `tasksByProject[boardId]` (`task.n` is display-only); current task = first non-complete, current subtask = first non-satisfied, everything after **locked-but-previewable**. Engine ([orientation-selector.js](src/data/orientation-selector.js)): new pure helpers (`isTaskComplete`/`isTaskSnoozedToday`/`findCurrentTaskIndex`/`findCurrentSubtaskIndex`/`taskPillState`/`subtaskChipState`) + `deriveBoardSequence`/`deriveSubtaskSteps`; `findFirstEligibleInPillarTier` **removed** for `findActiveBoardInPillarTier` (first actionable board by `localeCompare`, else snoozed fallback); `recommendOrientation` now requires `seq.actionable` so a **fully-snoozed pillar falls through to the next ranked pillar** — the necessity-tier triage (Daruriyyat → Hajiyat → Tahsiniyat) still picks the pillar, the chain only orders steps within it. The `buildOrientationCarousel` card is **additive** (adds `board`/`currentTaskIndex`/`currentSubtaskIndex`/`steps`; `hasEligible = board && board.actionable`) so the card faces are untouched. New [OrientationSteppers.jsx](src/components/orientation/OrientationSteppers.jsx) + [.css](src/components/orientation/OrientationSteppers.css) — lettered **Task pills** + numbered **Subtask chips**, state classes done/current/locked (+ latent `--snoozed`) and a `--previewing` ring, tokens-only (light+dark safe). [OrientationSheet.jsx](src/components/orientation/OrientationSheet.jsx) owns a `preview {taskIndex,subtaskIndex}` synced by the **render-time "adjust state during render" reset** (not a `useEffect` — resyncs before paint, dodges `set-state-in-effect`); previewing a locked step shows its detail with the primary **disabled + relabeled** (`Complete prior steps` ahead / `Completed` behind), while every handler in [Orientation.jsx](src/components/orientation/Orientation.jsx) writes the **true current step** off `openCard`. "Not today" now snoozes the whole **task** (`updateTask(project.id, task.id, { snoozedUntilDayKey })` — **no new store action**; `resetDailyCadenceTasks` spreads `...t` and clears subtask `done`, re-locking on the next Islamic day) with cross-board-then-cross-pillar fall-through. Defaulted guard props on [SubtaskStepDetail.jsx](src/components/shared/SubtaskStepDetail.jsx) (`taskStepper`/`subtaskStepper` slots) + [OrientationActions.jsx](src/components/orientation/OrientationActions.jsx) (`primaryLabel`/`disabled`) keep the **Prophetic Path node popup byte-identical**. One behavior-visible change: selection is array-order not priority-order (seed-order curation named as a follow-up; the `--snoozed` pill ships **latent** — a snoozed board is skipped, never surfaced, kept for model honesty). **Verified:** `npm test` **142/142** (selector suite drops the priority-order + `findFirstEligibleInPillarTier` cases, covers every new helper + `findActiveBoardInPillarTier` states + array-order selection + **cross-pillar fall-through** + the new card fields); composite `npm run lint` **fully green** (eslint 0, grounding-strict prayer 87/87 all-pillars conform, inline-refs 0 ≤ 0, `generate:pillar-glyphs:check` up-to-date 41 — hermetic since [[2026-07-26-milos-mojibake-repair-glyphs-loader-fix]]); `npm run build` ✓ 1.28s (only pre-existing `auth-store` dynamic-import + chunk-size warnings). **Screenshots could not be captured** — [[project-screenshot-hang]] recurred; verified via live DOM (a11y tree) + `localStorage` in the isolated preview browser (all 7 pillars seeded via `-core` routes to force both steppers): both steppers render (Task A–E, Subtask 1–5), locked subtask/task-pill preview disables+relabels, **Mark done** advances in place (chip → done, next → current, 0/5 → 1/5, sheet stays open), **Not today** writes `snoozedUntilDayKey` on the **task** (subtasks null) and rolls to `health_physical_core` (cross-board), **Doesn't apply** sets `notApplicable` with a neutral ack toast; board-close + cross-pillar fall-through are unit-covered. **Amanah neutral** — UI sequencing over existing halal task data, no capital/sale/riba/gharar/CSRA/salam/yield surface, Maqasid tier + pillar framing preserved. Uncommitted on `feat/desktop-pillar-glyphs`, commit operator-gated. See [[2026-07-26-milos-orientation-sequential-locking]]. |
| 2026-07-26 | **Repo hygiene: `.claude/worktrees/` gitlinks untracked, 5 agent worktrees swept, atlas set to `ignore = dirty`.** Six lines of `git status` noise that had persisted across several sessions turned out **not to be cosmetic**: six `.claude/worktrees/*` entries were committed as **gitlinks (mode `160000`) with no `.gitmodules` mapping**, making `git submodule status` **fatally error** and breaking every `git submodule` command — including `--init`/`update` for the real `atlas` submodule. The existing `.gitignore` rule `.worktrees/` had been **inert since written** (never matches `.claude/worktrees/`). Fixed in `269498a`: `git rm -r --cached .claude/worktrees/` (index-only), `.claude/worktrees/` added to [.gitignore](.gitignore) scoped so the 8 legitimate tracked `.claude/` files stay tracked, and `ignore = dirty` on atlas in [.gitmodules](.gitmodules). **Preserve-before-sweep:** chatelet's uncommitted `isPrayerLocked`→`prayerBannerActive` rename (`AppShell.jsx`, `threshold-store.js`, 2 `CONTEXT.md`) committed to its own branch as `a1e5892`; vigilant-buck's 136 untracked grounding-audit JSONs copied to scratchpad, count-verified, before a `--force` removal; the Health-mojibake ADR cherry-picked off `claude/sleepy-lamarr-3f62e8` as `86facd0`+`cfa4761` (3 predicted wiki conflicts, all additive single-row inserts, resolved in date order). **No branch deleted** — removing a worktree doesn't delete its branch; all 4 `claude/*` branches survive. atlas pointer **deliberately not committed** (HEAD `e4031e2` is on no remote → would break recursive clones); `ignore = dirty` still surfaces genuine pointer drift, so ` M atlas` correctly remains. Verified: `git submodule status` prints its single atlas line without fatal, `git worktree list` shows only main + `mwt/glyphgen`, atlas untouched (34 dirty, `e4031e2`), `npm test` 142/142. Residue: 2 empty dirs unlinkable under a Windows handle lock — already deregistered, invisible to git. **Amanah neutral** — git plumbing only. See [[2026-07-26-milos-worktree-gitlinks-atlas-ignore]]. |
| 2026-07-26 | **Seed mojibake repaired (proven byte-reversal) + glyph generator hermetic — composite lint fully green.** All 485 UTF-8/cp1252 occurrences in the 3 data files ([[project-seed-mojibake]] → Resolved) restored from parallel-session commit `9f87e8d` and independently proven byte-identical to the mechanical cp1252 reversal (strip BOM → C1-lenient encode → original UTF-8) — zero invented Qur'anic text; BOM gone; Hilali-Khan `â` residuals legitimate; gates untouched-green. The `generate:pillar-glyphs:check` 60s SSR transport timeout root-caused by probe bisection to `createServer` loading `vite.config.js` (NOT the lucide barrel — pre-bundling it only moved the failure); [scripts/generate-pillar-glyphs.mjs](scripts/generate-pillar-glyphs.mjs) now passes `configFile: false` + direct config import for `resolve.alias` only → check ~1.5s, exit 0 ×2 with the dev server running. First fully-green composite `npm run lint` on `feat/desktop-pillar-glyphs`; `npm test` 102/102. Uncommitted — ayat file pending operator covenant review. See [[2026-07-26-milos-mojibake-repair-glyphs-loader-fix]]. |
| 2026-07-26 | **Centered popups everywhere, orientation desktop stage+rail, node-popup drill-in.** Four operator requests after the carousel redesign, locked via AskUserQuestion: (1) the clipped "Weakest — recommended" badge fixed — `.orient-carousel` top padding `--space-1`→`--space-3` (the flag's −9px overhang vs. `overflow-x:auto` forcing computed `overflow-y:auto`, which clips above the scrollport; gotcha recorded); (2) the prior ADR's deferred **desktop layout delivered**: new stateless [OrientationSpread.jsx](src/components/orientation/OrientationSpread.jsx) gated by `useMobile()` — focused card large in a stage (the unmodified `OrientationCard`, which alone opens the sheet) + all-7 rail whose click swaps `focusPillarId`; 768–1023px wraps the rail as chips (`auto-fill, minmax(180px,1fr)`), ≥1024 a 236px column; mobile carousel zero edits; (3) the node popup **dropped its `TaskDetailPanel` hand-off** for an inline Orientation-style **drill-in** in [NodePhaseSlideUp.jsx](src/components/islamic/NodePhaseSlideUp.jsx) — `detailKey={projectId,taskId}` is a **key, not a snapshot** (row re-found in fresh `phaseTasks` each render; `findNextEligibleSubtask` picks the Now; `isSubtaskSatisfied` counts progress); actions hit the store directly (Mark done / Doesn't apply / Not today via `toggleSubtask`/`notApplicable`/`snoozedUntilDayKey` off `computeTodayKey(maghribRaw)` in an effect); continuity free — Now advances in place, last-step consumption shows the complete state with **no auto-nav**; crumb `DARURIYYAT › …` or bare "SALAH" for prayer rows (`_level:null`); `PropheticPath.jsx` lost the whole `selectedTask`/`openTask`/`TaskDetailPanel` plumbing; (4) **all four `pp-slideup__*` surfaces became centered modals at every viewport** — [ProjectSlideUp.css](src/components/work/ProjectSlideUp.css) is now the app-wide chrome (centered flex root, `min(92vw,620px)`×85vh panel, radius 20, `pp-slideup-pop` fade+rise entrance, `--wide` `min(94vw,1100px)`×88vh variant), consumers reduced to deltas, the load-bearing `display:contents` portal rule untouched, `ProjectSlideUp`/`SubmoduleSlideUp` moved to `useFocusTrap`. **Step internals shared:** the sheet body lifted verbatim (class names kept) into [shared/SubtaskStepDetail.jsx](src/components/shared/SubtaskStepDetail.jsx) — consumed by both the orientation sheet and the popup drill-in, so the step UX cannot drift; `OrientationEvidence`→`shared/SubtaskEvidence.jsx`; `.orient-actions*`→own CSS; `computeTodayKey`→[src/utils/islamic-day-key.js](src/utils/islamic-day-key.js). Naming kept (`pp-slideup→pp-popup` rename deferred). **Verified** light+dark, mobile+desktop, **screenshots working**: `npm test` 102/102, eslint 0 errors, grounding-strict ✓, inline-refs 0; composite `npm run lint` red **only** on the pre-existing environmental `generate:pillar-glyphs:check` Vite SSR RunnerError — retried at Phase 5 per the operator's decision, still failing, unrelated. Full drill-in flows exercised on Fajr (prayer) + midday-labor (non-prayer): advance-in-place, snooze-without-count, complete state, Back, tab-clear; desktop stage+rail swap; dark via `data-theme` incl. the portal root's copy; ~900px rail chips. Deferred: PPTaskCard done-count vs. `isSubtaskSatisfied` mismatch, stacked-dialog Escape order, ack toasts. **Amanah neutral** — presentational, surfaces existing graded evidence, no capital/CSA/CSRA/salam/yield-share. Uncommitted on `feat/desktop-pillar-glyphs`, commit operator-gated. See [[2026-07-26-milos-centered-popups-node-drill-in]]. |
| 2026-07-26 | **Reversed pre-existing UTF-8/cp1252 mojibake in the three Health-pillar data files (incl. Qur'anic verse data).** Double-encoding corruption surfaced during `/app/orientation` verification — the Health morning-routine title rendered `Fajr â†' Quran ...` (the arrow `→` U+2192 corrupted to `â†'`). Affected [health-seed-tasks.js](src/data/seed-tasks/health-seed-tasks.js), [health-readiness-ayat.js](src/data/ayat/health-readiness-ayat.js) (**⚠️ Qur'anic verse data**), and [niyyah-feelings.js](src/data/niyyah-feelings.js) (also carried a UTF-8 BOM). **Root cause (git-pinned):** commit `cfad9dd` re-saved three files whose UTF-8 bytes had been misread as Windows-1252 then re-encoded as UTF-8 (that commit also renamed Life→Health); parent `1171fc5` holds the clean bytes as the `life-*` files. **Fix:** deterministic round-trip `decode('utf-8').encode('cp1252').decode('utf-8')` — the exact inverse, preserving `cfad9dd`'s Life→Health rename — with a `c1passthrough` codec handler for the 5 cp1252-undefined bytes Arabic continuation bytes hit, BOM stripped; written UTF-8 no-BOM, LF preserved. **Amanah sensitive** (Qur'anic): revelation text **restored from the pre-corruption git blob**, never guessed; byte deltas reconcile exactly with the rename (ayat +6/seed +26/niyyah +6); decoded Arabic byte-counts **== parent exactly** (4201/14906/96); on-disk ayat vs `1171fc5:life-readiness-ayat.js` = **6 changed lines, all the `LIFE`→`HEALTH` rename**, all **35 Arabic lines byte-identical**. Verified: mojibake re-scan 0; `git diff --stat` 3 files 784/784 in-place; `npm test` **77/77**; `npm run lint` eslint + grounding-strict (`[STRICT] Pass`, Health 87/87) + inline-refs (`0 ≤ 0`) green. **Pre-existing unrelated:** `lint`'s `generate:pillar-glyphs:check` fails `ENOENT lucide-react/.../compass.js` — proven identical with the fix stashed; this worktree's `node_modules` lacks `lucide-react/dist/esm/icons` (dependency-install gap, `npm install` fix, deferred). Commit `9f87e8d` on `claude/sleepy-lamarr-3f62e8`; operator cleared the ayat review gate before commit; **not pushed**. ADR: [[2026-07-26-milos-health-data-mojibake-repair]]. |
| 2026-07-25 | **`/app/orientation` reskinned — carousel of 7 Maqāsid domain cards + bottom sheet.** User testing found the shipped ladder + balance-strip surface ([[2026-07-23-milos-orientation-screen]]) hard to act on — everything visible, nothing telling you what's next. An operator prototype proposed a **carousel** (the weakest pillar centred + flagged, the other six one swipe away) with all depth moved into a **bottom sheet**. Exploration found the recommendation **engine already shipped**, so this was a **UI reskin over a proven engine**, not a rebuild. Four decisions locked via AskUserQuestion: redesign **in place** (reuse engine); **defer** streak counter + "welcome back" banner (conflicts with the locked no-streak/guilt decision + needs absent gap-detection); **extend the engine** for priority-aware task selection; **mobile-first** centred 520px column (defer desktop). Scope held: standalone route only — `Dashboard.jsx`/`/app` home untouched, orientation **not** made the home. **Engine** ([orientation-selector.js](src/data/orientation-selector.js)): `PRIORITY_RANK`; `findFirstEligibleInPillarTier` now sorts a pillar+tier's tasks by **priority then `order`** (was `order` only); new `buildOrientationCarousel(...) → {cards, recommendedPillarId}` yields the 7 `MAQASID_CORE_PILLARS` in canonical order with `recommendedPillarId` derived from `recommendOrientation` (inherits tier fall-through). **UI:** retired `OrientationLadder`/`OrientationBalanceStrip`; new `OrientationCarousel` (scroll-snap + dot row), `OrientationCard` (one `<button>`: Arabic+English name, crumb, priority pill, "Now" line, progress bar, recommended + caught-up variants), `OrientationSheet` (bottom sheet on the **shared** `pp-slideup__*` chrome + `useFocusTrap` — portal/`role=dialog`/`aria-modal`/Escape; Why&how + Evidence accordions; three-action footer). `OrientationActions` trimmed 4→3 (**dropped "Something else" — the carousel *is* the pillar picker**). **Container** ([Orientation.jsx](src/components/orientation/Orientation.jsx)) keeps effect-driven recompute; held-task continuity now sheet-level via `openPillarId` + `pendingRef`; `dayKeyRef` routes the wall-clock read out of render — **which also cleared the pre-existing `Date.now()` purity lint error** the prior orientation ADR left reddening the composite gate. The **tier gate proved live**: with all seven pillars seeded, **Health** (0/96 — weakest active-tier ratio) was flagged "Weakest — recommended," chosen over Faith (1/139) by *ratio*; its leading task happens to be *urgent* and Intellect's *high*, but priority only orders tasks **within** the pillar the ratio already picked. **Amanah neutral-to-positive** — presentational, surfaces existing graded evidence, authors no fiqh, no capital/CSA/CSRA/salam/yield-share. `npm test` **102/102** (new priority-order + carousel coverage), `npm run build` ✓, `npm run lint` **green across all three gates** (no seed edits → ratchets held). **The screenshot tool worked this session** (contrast the recurring [[project-screenshot-hang]]) — carousel **light** + open **Health sheet** + carousel **dark** all captured, token theming flips cleanly; DOM/behavior corroborated with **0 console errors**. **Surfaced out-of-scope:** pre-existing UTF-8/cp1252 **mojibake** (corrupted `→`/apostrophes/dashes) — **485 occ across 3 data files** incl. `health-readiness-ayat.js` (⚠️ Qur'an data) — flagged as `task_2e56b6bf`, tracked at [[project-seed-mojibake]]. Uncommitted on `feat/desktop-pillar-glyphs`, commit operator-gated. See [[2026-07-25-milos-orientation-carousel-redesign]]. |
| 2026-07-25 | **Every prayer node's During tab now has a "how to pray" guide.** Third same-day prayer-popup session — it completes the During tab that [[2026-07-25-milos-prayer-popup-consolidation]] inlined, which still showed a coming-soon shell on four of six nodes. Operator: *"Every prayer node in the prophetic path is missing the how-to in the during tab except for Isha. Let's fix that."* Premise slightly off (Fajr had one, masked by the shell's stale *"Isha is available today"* copy) → real gap was **Dhuhr, Asr, Maghrib, Tahajjud**. Chosen via AskUserQuestion: scope all four, **shared builder**, author matching corpora. New [prayer-during-builder.js](src/data/prayer-during-builder.js) holds recitation objects **copied verbatim** from the verified [isha-during.js](src/data/isha-during.js); `buildPrayerSequence({rakahCount, audibleRakahs, closingNote})` encodes the standard sequence and **computes** `posturesPerRakah`. Four ~6-line files register in [prayer-sequences.js](src/data/prayer-sequences.js): Dhuhr/Asr (4-rakʿāt silent), Maghrib (3-rakʿāt, aloud r1–2), Tahajjud (2-rakʿāt **nafl** unit + pairs-of-two/Witr closing note); Isha/Fajr untouched. **Tahajjud "Fard" guard:** the header hardcodes *"How to pray {label} **Fard**"* (a fiqh error for a nafl prayer) → optional `headline` override renders **"How to pray Tahajjud — 2 rakʿāt at a time."** Four provenance-only `nb_salah_{dhuhr,asr,maghrib,tahajjud}.json` corpora reuse Isha's 12 entries byte-identical, `structural_notes` citing in-repo sources (Bukhari 990/Muslim 749; Bukhari 1147/Muslim 738; Muslim 752). **Amanah positive** — no invented hadith/Arabic, structures confirmed against `PRAYER_GUIDE`, refuses to call a nafl prayer Fard. `npm test` 94/94, `npm run build` ✓ 1.33s, the three documented lint gates green (eslint 0 errors, grounding-strict prayer 87/87, audit:inline-refs prayer 0). Composite `npm run lint` red **only** on the 4th step — branch-local WIP `generate:pillar-glyphs:check` SSR-timeout on a **health-pillar** file ([HealthLevelNavigator-constants.js](src/pages/health/HealthLevelNavigator-constants.js)), structurally unrelated and the same nondeterministic Vite-transport failure proven not-mine last session (Family→Health file change confirms it). **Screenshot tool unavailable** ([[project-screenshot-hang]]) → DOM-verified: all four render During-default, zero mojibake; critical check passed — Tahajjud header **no "Fard"** (`headerSaysFard: false`), closing note + Pray-Along intact. Uncommitted on `feat/desktop-pillar-glyphs`. See [[2026-07-25-milos-prayer-during-guides-all-nodes]]. |
| 2026-07-25 | **Non-prayer node popup — tasks consolidated onto During, Before/After reduced to ceremony previews.** Same-day, inverse-scope complement to the prayer-popup consolidation below (which had left non-prayer nodes untouched). Operator: *"Move the task list from before and after to the during tab"* — scope confirmed **non-prayer nodes only**, and **Before/After keep the `CeremonySummary` ceremony preview** (only the task list leaves). Non-prayer nodes had been splitting their pool across the three tabs by keyword `phaseMatchers` (Before `/plan|planning|prep/`, After `/wrap|close/`, During the `main` remainder) — which is why the operator's "Plan the week's deep-work" task rendered under **Before**. In [NodePhaseSlideUp.jsx](src/components/islamic/NodePhaseSlideUp.jsx) the `phaseTasks` non-prayer branch now calls `buildTasksForNode(node.id, …, { phase: null, limit: 20 })` — `phase: null` drops the split so During shows the **combined** pool, `limit` 8→20 so the merged list isn't truncated; `SLOT_BY_PHASE`/`slot` removed as dead. Before/After render `CeremonySummary` alone (the `<div className="pp-phase-tasks">{taskList}</div>` wrapper gone, its orphaned CSS rule deleted from [NodePhaseSlideUp.css](src/components/islamic/NodePhaseSlideUp.css)). **`midday-labor`** — the one non-prayer node whose During showed a `showProjects` Projects/Education view — was regularized per operator choice to show the task list; `showProjects`/`scopeProjects` and unused imports (`buildUserProjectsForScope`, `submodulesForNode`, `getPillarSubmoduleIds`) removed, During passes `showProjects={false}`. `MirrorCard`'s `showProjects` branch left intact in [PropheticPathMirror.jsx](src/components/islamic/PropheticPathMirror.jsx) — reusable export, never reached now. `npm test` 94/94, ESLint on the edited file 0 problems, `npm run build` ✓ clean. Full `npm run lint` still red **only** on the pre-existing unrelated `Date.now()` purity error in [Orientation.jsx:42](src/components/orientation/Orientation.jsx) ([[2026-07-23-milos-orientation-screen]]), not touched here. **The screenshot tool was unresponsive this session** (the recurring [[project-screenshot-hang]] failure) — DOM-verified: Midday Labor During → 20-card task list, no `.pp-project-list`, both weekly-planning tasks present; Before → "Begin opening" ceremony, 0 task lists; After → "Begin closing", 0 task lists; Fajr (prayer regression) → During still the inline guide, Before still the bare task-list branch. **Amanah:** neutral — presentational/navigational, no seed/grounding/fiqh/capital surface. Uncommitted on `feat/desktop-pillar-glyphs`. See [[2026-07-25-milos-nonprayer-tasks-to-during]]. |
| 2026-07-25 | **Prayer node popup consolidated — tasks-only Before/After, the prayer guide inlined into During.** Two operator corrections in two turns: *"I only want to see the list of tasks and not the list of prayers"* and *"the during tab should contain the content it currently refers/links to."* On the six prayer nodes in [NodePhaseSlideUp.jsx](src/components/islamic/NodePhaseSlideUp.jsx), **Before/After** now render the bare task list — `PrayerSunnahSummary` (the rawātib card from the 2026-07-23 Sunnah-tabs decision) is deleted, reversing that display; the tab label carries the phase context. **During** renders `<PrayerHeroDuring pillarKey={node.id} />` **inline** (the full during-the-prayer guide + opt-in Pray-Along), replacing the *"Open prayer phases"* hand-off button. Non-prayer nodes untouched (`MirrorCard` on During, `CeremonySummary` + task divider on Before/After). Deleting the hand-off orphaned a chain — **six files removed** after grepping each for importers: `PrayerSlideUp` (+CSS), the cloned `PrayerLevelPage` + `PrayerLevelNavigator`, and `PrayerSunnahSummary` (+CSS) — **partially superseding** the [[2026-04-21-prayer-slide-up-fln]] decision (its 18 boards + Option-A seed model survive; only the cloned components die, and the During-hero it introduced now renders inline in the popup). `PrayerHeroDuring` is now consumed **only** by the popup. `PRAYER_GUIDE` / `getPrayerPhaseSunnah` / `SUNNAH_LEAD` are **retained but unrendered** — they still seed the `prayer_{id}_during` anatomy boards, so the graded data stays in-repo; **authors no fiqh.** `npm test` 94/94, ESLint on the four edited files 0 errors, `npm run build` ✓ (no dangling imports); the full `npm run lint` still red only on the pre-existing unrelated [Orientation.jsx](src/components/orientation/Orientation.jsx) `Date.now()` purity error. **The screenshot tool worked this session** (contrast the prior three) — Fajr Before/After tasks-only, During inline guide + Pray-Along inside the panel, Dhuhr coming-soon fallback, and the non-prayer node non-regression all verified. **Amanah:** neutral-to-positive — deepens the During tab, declutters Before/After, removes and authors no graded content. Uncommitted on `feat/desktop-pillar-glyphs`. See [[2026-07-25-milos-prayer-popup-consolidation]]. |
| 2026-07-23 | **New `/app/orientation` screen** — single-recommendation "what's next" surface (one tier-ranked subtask, 5-rung breadcrumb ladder, inline evidence, 4 non-punitive actions) built across 4 phases from an operator concept mockup. Necessity-tier ranking never blends tiers system-wide; "Not today" is a real snooze via `snoozedUntilDayKey`. `npm test` 94/94, lint clean, build green; screenshot tool unavailable, DOM-verified instead. See [[2026-07-23-milos-orientation-screen]]. |
| 2026-07-23 | **Prayer-phase tasks were invisible, and Tahajjud's Before was showing the prayer itself.** Operator: *"there's so much more to the before of Tahajjud that's missing from here"* — the tab showed one `Qiyām · Pairs of 2` card above "No tasks queued for this window." **Two unrelated defects on one screen.** (1) A seeded 4-subtask *"Rise for Tahajjud with the prophetic waking protocol"* (wipe sleep + Āl-ʿImrān 3:190–200, the waking duʿāʾ, siwāk, istiftāḥ) was in the store all along: `PRAYER_BOARDS` ([prayer-pillars.js](src/data/prayer-pillars.js)) ships all 18 boards with **`moduleId: null`**, and `buildTasksForNode`'s project filter keeps only boards whose `moduleId` resolves to a canonical submodule — so `null` dropped **all 49 seeded prayer-phase tasks**, every prayer, every tab, since the popup shipped. Fixed by reading `prayer_{id}_{phase}` **directly** (`buildPrayerPhaseTasks`) rather than inferring: setting `moduleId` at source would need a storage migration and would bleed the boards into every other faith-salah surface, and id-pattern resolution inside the pipeline would let Tahajjud's keyword matchers (`/witr|siwak|rawatib/`) pull *other prayers'* tasks into its windows. Caught inside the fix: `levelFromProjectId` **defaults to 3**, so prayer tasks would have been chipped "L3 · Tahsiniyyat" — calling Fajr's *muʾakkadah* rawātib an embellishment; `_level: null` + a conditional chip in [PropheticPathMirror.jsx](src/components/islamic/PropheticPathMirror.jsx) instead. (2) `PRAYER_GUIDE.tahajjud.structure` has **no `Sunnah before` row**, so the previous day's selector mapped Before → the `Qiyām` row, offering the night prayer as what precedes the night prayer. Operator chose "keep it, reframed": a `SUNNAH_LEAD` table surfaces the two unused `keys[]` entries (last-third descent hadith; *"Begin with 2 light rakʿahs"*) as `leadNotes[]` above the row, captioned **"What you rise toward"** — nothing lost, the ordering teaches. Still **authors no fiqh**; both notes are existing graded strings verbatim. `npm test` 77/77, ESLint 0 errors, both grounding ratchets 0, build ✓ 1.49s. **The screenshot tool did not work** ("Browser pane is not displayed, so the page is not compositing frames") — verification was live-DOM and said so. Committed `4ec016e`. See [[2026-07-23-milos-prayer-phase-task-boards]]. |
| 2026-07-23 | **The six prayer nodes stopped sharing one du'a — each now shows its own before/after Sunnah.** Operator, opening the new node popup: *"Each prayer has a unique Sunnah before and after. Identify and present before updating."* Root cause: `THRESHOLD_MODULE_BY_NODE` maps all six prayer nodes to `faith-salah`, so `<CeremonySummary moduleId="faith-salah">` rendered the same threshold twelve times (6 nodes × Before/After). The per-prayer data **already existed and was already hadith-graded** in the private `PRAYER_GUIDE` ([prayer-seed-tasks.js](src/data/seed-tasks/prayer-seed-tasks.js)), used until now only to seed the `prayer_{id}_during` anatomy boards — so **this change authored no fiqh**, which is exactly why both grounding ratchets stayed at 0. New `getPrayerPhaseSunnah(prayerId, phase)` selector (guide stays private) returns `rows[]` — an **array**, because Isha's "after" legitimately carries two prayers, the 2-rakʿah rawātib *and* Witr. New [PrayerSunnahSummary.jsx](src/components/islamic/PrayerSunnahSummary.jsx) + co-located **unscoped** CSS (portal requirement, per `CeremonySummary.css`) renders count badge + tier pill (T1 *Muʾakkadah* / T2 *Ghayr muʾakkadah* / T3 *Optional*) + note + why + Arabic/translation/`ref · grade`; `valuesLayer: universal` suppresses the Arabic. Four windows have **no** rawātib and are surfaced honestly as prohibition/permission notes from `keys[]` rather than padded — Fajr and ʿAṣr "after" are *prohibitions*, Maghrib "before" a permission, Tahajjud "after" the no-two-witrs rule; a symmetric card would teach a prayer that should not be prayed. Fallback needles kept **all-ASCII** (`startsWith('Qiy')`, not `'Qiyām'`) so a cp1252 checkout cannot silently blank a card. Non-prayer nodes keep `CeremonySummary` unchanged; the niyyah ceremony stays reachable via the route-level `CeremonyGuard`. `npm test` 77/77, grounding gate green, `npm run build` ✓ 1.47s, screenshot-verified incl. the `midday-labor` non-regression, universal layer, and 375px. See [[2026-07-23-milos-prayer-node-sunnah-tabs]]. |
| 2026-07-22 | **Prophetic Path node popup — one Before/During/After entry point per node.** The `.pp-satellite` Before/After buttons were the only route to a node's opening/closing threshold and `PropheticPath.css` **hid them on every non-active node**, so on almost the whole day's timeline — everything past, everything upcoming — the thresholds were unreachable. Clicking any node card now opens [NodePhaseSlideUp.jsx](src/components/islamic/NodePhaseSlideUp.jsx), a slide-up with a three-tab pill switch defaulting to **During**: Before → threshold preview + that phase's tasks; During → `MirrorCard`, or on prayer nodes a hand-off button into `PrayerSlideUp`; After → closing preview + tasks. The **inline spine mirror was removed** — operator chose "Option A, clean timeline" from a side-by-side, so the spine reads as a day and only marks the current node as *now*. The ceremony is **not** rendered locally: "Begin opening/closing" sets `openingModuleId`/`closingModuleId` on threshold-store and closes the popup, handing off to the globally-mounted `ThresholdModal`. Supporting extractions: [PropheticPathMirror.jsx](src/components/islamic/PropheticPathMirror.jsx) (`MirrorCard`/`PPTaskCard`/`EducationList`/`ProjectRow`) and [prophetic-path-constants.js](src/components/islamic/prophetic-path-constants.js) — which imports from neither consumer, and that is what breaks the circular import; plus `resolveCeremonyData(moduleId, valuesLayer)` exported from [islamic-data.js](src/data/islamic/islamic-data.js) so the preview applies the same pillar fallback the ceremony will (`MODULE_ATTRS` is pillar-keyed, so `faith-salah` resolves to `null` alone). `PropheticPath.jsx` −437 lines, `PropheticPath.css` −51. **Portal gotcha, recorded because it fails silently:** the popup portals into `<body>` where every `.prophetic-path`-scoped rule evaporates, so the portal root carries `className="prophetic-path pp-phase-slideup"` + `data-theme` and `.pp-phase-slideup.prophetic-path { display: contents }` strips the root's 100vh box while custom properties still inherit — drop either half and the styling vanishes with no error. See [[2026-07-22-milos-prophetic-path-node-popup]]. |
| 2026-07-10 | **Prayer-time banner copy reverted to the original tone.** Follow-up to the 07-09 de-lock ([[2026-07-09-milos-prayer-banner-non-blocking]]). Operator: *"I prefer the original wording for the messages attached to prayer time."* The two `PrayerOverlay` prompt strings **only** — before the adhan *"It's almost time for {prayer}."* → **"{prayer} is approaching."**; at/after `prayerTimeMs` *"It's time for {prayer} — pray when you're able."* → **"It is time for {prayer}."** Prayer name **kept** (operator choice via AskUserQuestion); the slim banner has no separate heading. `PrayerWarning` **left unchanged** by a second explicit operator choice — its *"screen will pause"* text is no longer true, so reverting it would put a false claim back on screen. The theological *approaching → it is time* flip is preserved verbatim in structure (fard never declared "time" during the 5-min `PRAYER_LEAD_MS` lead). Verified: ESLint 0 errors, build green, both states confirmed via live DOM (`"Fajr is approaching."` → `"It is time for Fajr."`); `preview_screenshot` timed out at 30 s again — no visual capture, disclosed. One file (`src/components/islamic/PrayerOverlay.jsx`), **not committed** (operator-gated). |
| 2026-07-09 | **Prayer overlay finished its de-lock — now a slim, non-blocking top banner.** The operator reported a prayer-time feature that "blocks part of the screen [and] can't be removed until prayer time is over." The wiki said otherwise: the 2026-04-22 session already de-locked `PrayerOverlay` (fullscreen scrim + focus trap + `aria-modal` → `pointer-events: none` card, `role="status"`), and `HEAD~1` confirms it. **There was no lock — four surviving *signals* of one:** (1) `PrayerWarning` copy literally promised *"screen will pause in N minutes"*; (2) `handlePrayerDismiss` chained into `triggerResume(activeModule)`, so pressing the only exit mounted `ResumeOverlay` in the banner's place — the mechanism behind "can't be removed"; (3) the card was a 320px `rgba(0,0,0,0.92)` near-opaque black panel (`pointer-events: none` is click-through, not invisible) — "blocks part of the screen"; (4) a large `mm:ss` countdown framed it as a timer to wait out. Stale `CONTEXT.md` + the still-extant `isPrayerLocked` flag name kept re-teaching the lock to every reader (two explore agents mis-reported it as a "full-screen blocker" this session). Fix: top-centre pill on the project token set, dismissible via `الحمد لله · Alhamdulillah` **or** a new `✕`, auto-clearing at `prayerTimeMs + PRAYER_TRAIL_MS`; countdown deleted (per-second `setInterval` → two `setTimeout`s); `ResumeOverlay` chain cut; `minutesUntilLock` prop deleted. **The "almost time" → "it's time" flip at `prayerTimeMs` is theological, not cosmetic** — the banner mounts `PRAYER_LEAD_MS` (5 min) *before* the adhan, and fard may not be prayed before its time enters; it survived a `react-hooks/set-state-in-effect` fight (initial value from the `useState` initializer, flip inside the `setTimeout`, no in-place reset needed since AppShell remounts per prayer). Verified: ESLint 0 errors, build green, `elementFromPoint` hit-tests + a live sidebar click that changed route with the banner mounted; a 375px shrink-wrap defect (188×143px column) found and fixed mid-verification with a `@media (max-width: 640px)` block (→ 351×77px). **`preview_screenshot` timed out 6× at 30s — no visual capture exists; disclosed, not assumed.** Amanah neutral (and removes a false claim the app was making to its user). **Debt: `isPrayerLocked` is now a misnomer — rename to `prayerBannerActive` when a session next touches `threshold-store.js`.** Committed `e4302ce` on `feat/desktop-pillar-glyphs`, not pushed. ADR: [[2026-07-09-milos-prayer-banner-non-blocking]]. |
| 2026-07-09 | **Verse banner inset past the right chrome + equal-height rounded Niyyah footer buttons.** Two operator-requested app-shell UI fixes. **NiyyahAct** ([NiyyahAct.css](src/components/islamic/NiyyahAct.css)): `.niyyah-footer` `align-items:center`→`stretch` so both footer buttons take the tallest height; `.niyyah-skip` gains `display:flex; align-items:center; justify-content:center;` to re-center its label; both `.niyyah-confirm`/`.niyyah-skip` `border-radius: var(--radius-md)`→`var(--radius)`. **Gotcha:** `--radius-md` is undefined repo-wide (0 defs in `src/`) → invalid declaration → `border-radius` fell back to initial `0` (square); `--radius` (10px) is the real "medium" token, so the swap is what delivers the rounded corners. **TopBar verse banner** ([TopBar.jsx](src/components/layout/TopBar.jsx)): inline `right` changed from `islamicPanelOpen && !mobile ? \`${islamicPanelWidthPx + 28}px\` : 0` to `mobile ? 0 : \`${28 + (islamicPanelOpen ? islamicPanelWidthPx : 64)}px\``. The banner is `position:absolute; left:var(--edge-w); right:0` inside the `grid-column:2/-1` topbar; when the Islamic panel was closed the old `right:0` ran it to the viewport edge over the 28px right-edge column + 64px rail — the new value always insets past the right chrome (28px edge + panel-or-64px-rail), mirroring the left `var(--edge-w)` inset; mobile keeps `right:0`. Amanah neutral. Verified: implementing session (2026-07-04) live-verified both (Niyyah equal 42px height + 10px radius; banner aligned to main column, no overlap) by computed-style + `preview_screenshot`; rebuilt branch lint/tests/build green (77/77). **Reconciliation:** the branch's earlier PTE-CTA wiring commit + draft ADR were **superseded** — `origin/main` independently landed functionally-identical wiring (7 siblings + Faith) via PR #20 ([[2026-07-03-milos-pte-cta-wiring]]); `git diff` confirmed identical `route:` + `onClick` (whitespace-only), so the duplicate was dropped and the branch rebuilt on current `origin/main` with only these two fixes (`d720df9`). PR [#25](https://github.com/onaxyzogden/Maqasid/pull/25). ADR: [[2026-07-09-milos-topbar-banner-inset-niyyah-buttons]]. |
| 2026-07-05 | **Pillar-glyph CSS becomes a source-of-truth generator (B→A), extended to sub-pillars + prayers.** New `scripts/generate-pillar-glyphs.mjs` owns the glyph rules inside `/* BEGIN/END GENERATED */` markers in **both** media blocks of [level-navigator-responsive.css](src/styles/level-navigator-responsive.css). It SSR-loads app source (Vite `createServer` middlewareMode + `ssrLoadModule`) to read the 7 pillars' `MAQASID_CORE_PILLARS[].icon` strings (auto-propagate promise), 28 sub-pillars' `Icon.displayName` off each `<Pillar>LevelNavigator-constants.js`, and 6 prayers off `prayer-pillars.js`, then reads lucide `__iconNode` and encodes each to a data-URI (only `<>#` escaped; CheckCircle2→CircleCheck alias via displayName). **41 `data-pillar-id`s** (7+28+6) × 2 blocks = 82 rules. `generate:pillar-glyphs:check` appended to `lint` as a fail-on-drift ratchet. Since CSS isn't linted, `git diff` is the gate: **B1** drove the 7-pillar diff to empty (idempotent); **A** is purely additive (7 pillar URIs byte-unchanged; only 3 `ummah` delimiter flips + a stale-comment rewrite). Amanah neutral. Verified: build/lint/tests green (`--check` 41 glyphs up-to-date); DOM getComputedStyle proof on dashboard 7 pillars + health 4 sub-pillars at both breakpoints; all 41 ids × 2 blocks present in served CSS (`missing: []`). Prayer live-render + screenshots blocked environmentally ("Prayer times unavailable" guest state; screenshot channel down — [[project-screenshot-hang]]) — disclosed, not claimed. 4 commits on `feat/pillar-glyph-generator` off `origin/main`, **push/PR operator-gated (NOT pushed)**. ADR: [[2026-07-05-milos-pillar-glyph-generator]]. |
| 2026-07-05 | **Desktop dashboard pillar strip gets grouped icon+label glyphs (Option B).** Extended the mobile glyph swap ([[2026-07-04-milos-dashboard-pillar-icons]]) to the ≥768px `LevelNavigator` strip via a new disjoint `@media (min-width:768px)` block in [level-navigator-responsive.css](src/styles/level-navigator-responsive.css): each of the seven dashboard `data-pillar-id`s gets its `.fln__segment-nav` label button set to a centered block with a grouped glyph above the name (`.fln__segment-nav::before`, reusing the seven mobile lucide data-URIs, `--seg-color` fill, 18px). **Load-bearing:** `white-space:nowrap`+`text-overflow:ellipsis` (not wrapping) keeps the content-sized strip from collapsing its `flex:2.8` `.fln__center` grandparent to a ~252px sliver — strip stays stable at ~560px, three longest names ellipsize. Amanah neutral (presentational CSS). Verified: build/lint/tests green; strip 560px stable, 7 distinct glyphs, mobile unregressed. Shipped on `feat/levelnav-desktop-glyphs` (PR into `main`, best merged after PR #22). ADR: [[2026-07-05-milos-desktop-pillar-glyphs]]. |
| 2026-07-04 | **Dashboard mobile pillar strip shows canonical icons instead of `01`–`07` badges.** The ≤767px dashboard seven-pillar strip (`MaqasidLevelOverview.jsx`, `/app`) rendered ordinal `01`–`07` badges ([[2026-07-02-milos-levelnav-dtc-card-rows]]); every other pillar surface (sidebar, PillarCards, onboarding, mobile nav) uses the pillar's lucide glyph, so the numbered list was the lone inconsistency. User delegated the UX call ("icons instead of numbers if you think that's best") — judged icons win. CSS-only in the same global override [src/styles/level-navigator-responsive.css](src/styles/level-navigator-responsive.css) (no `@ogden/ui-components` fork; the package drops the `Icon` prop): for the 7 dashboard `data-pillar-id`s only, the counter-badge `::before` is swapped for the pillar glyph painted by masking `--seg-color` (the level tint) through the icon stroke shape → a bare tinted glyph like the sidebar. Glyphs (Compass/HeartPulse/Brain/Users/ChessRook/TreePine/Shapes) generated from [src/data/maqasid.js](src/data/maqasid.js) via `scratchpad/gen-pillar-icons-css.mjs`. Keyed on the 7 top-level ids (disjoint from sub-pillar/prayer/BBOS ids), so those strips keep their numbers; specificity (0,4,1) beats the (0,3,1) badge + 479px rules. Amanah neutral. Verified: build green; computed `::before` all 7 = `content:""` + mask 76% + fill `rgb(200,169,110)` (gold core); per-glyph canvas rasterization distinct (24–40% coverage, none blank/overfilled); `preview_screenshot` hit the 30s [[project-screenshot-hang]] (disclosed — proof programmatic). Committed `1c00324` on `feat/dashboard-pillar-icons` off `main`, PR [#21](https://github.com/onaxyzogden/Maqasid/pull/21). ADR: [[2026-07-04-milos-dashboard-pillar-icons]]. |
| 2026-07-03 | **Path-to-Excellence CTAs wired across all 8 pillar modules.** The `foundation`/`obligation`/`aspiration` CTAs on the seven non-Faith pillar overview cards were inert stubs (no `onClick`); Faith had just been wired + its overview decluttered (Comparison Wheel removed from `FaithLevelOverview.jsx`, PTE cards kept). Applied Faith's **self-contained** pattern to all 7 siblings (`useNavigate` + hardcoded `route:` literal per card + `onClick={() => navigate(route, { viewTransition: true })}`) — necessary because shared `LevelOverviewPage` renders `ExcellenceCardsComponent` prop-less. 24 proven-registered destinations (⚠️ Environment Foundation → `env-resource` over `env-waste`, title-primary, flagged). Amanah neutral (nav-only to existing boards; no capital/CSA/CSRA/salam/yield-share). Verified: lint 0 / tests 77/77 / build green; 7-module live click-through (all 3 route shapes) + grep on the other 14 literals; `preview_screenshot` hit the 30s hang (disclosed). Uncommitted working-tree (7 siblings + 2 Faith template files); commit operator-gated. ADR: [[2026-07-03-milos-pte-cta-wiring]]. |
| 2026-07-02 | **LevelNavigator mobile rows restyled as DashboardTaskCard-style cards (Variant A).** Follow-up to the 2026-07-01 stacked rows: the user asked the mobile strip to match `DashboardTaskCard`'s style/layout/color-status and chose **Variant A — two-row card** from a rendered mockup. Same global stylesheet + doubled `.fln__segments.fln__segments` anchor, CSS-only: each segment at ≤767px becomes a grid card (areas `'num label' / 'bar bar'` — DOM-order independent since the package renders the bar first) with a CSS-counter badge **01–07** using the exact `.dtc__card-num` accent formula (`color-mix(in srgb, var(--seg-color, var(--accent)) 14%, transparent)` + seg-color text), `--surface`/`--radius-lg`/`--shadow-card` chrome, bold de-uppercased title, and a full-width **44px** bar on a `--bg3` track. Package modifiers re-stated at (0,3,0)+: `--current` → 8% tint + seg-color border; wheel-hover sync → 70% border mix + `--shadow-card-hover`; ≤479px compaction (22px badge); `prefers-reduced-motion` re-statement. Verified at 375px light+dark (screenshots), real `--current` on `/app/faith-salah`, subsegs exactly 44px, inline `taskColorFn` backgrounds untouched, desktop 876px inert, 360×640 compaction; lint+build green. Discovery: the app-level `html.reduce-motion` `!important` rule ([[2026-04-29-milos-preview-and-dashboard-centering]]) zeroes the card transition live. Committed `d3941c3` on `feat/levelnav-mobile-dtc-cards`, PR [#19](https://github.com/onaxyzogden/Maqasid/pull/19) — **merged to `main` 2026-07-02** (merge `cd1997f`). ADR: [[2026-07-02-milos-levelnav-dtc-card-rows]]. |
| 2026-07-02 | **AppShell made lazy; Suspense relocated into its Outlet.** Executes the route-lazy session's top deferred item the same day. `AppShell` → `lazy()` in App.jsx; the shell hosts its own `<ChunkErrorBoundary><Suspense fallback={<RouteSpinner/>}>` around `<Outlet/>` inside `<main id="main-content">`, so inner-route chunk loads spin in the main column while the chrome (Sidebar/TopBar/MobileNav/Islamic rail) never unmounts. The 5 remaining eager work components (`CeremonyGuard`/`CeremonyGuardDynamic`/`ProjectBoard`/`AssetsTab`/`ProjectJournal`) → lazy, evicting `@dnd-kit` from the entry graph. Landing gained an idle preload (`requestIdleCallback` timeout 3000 + Safari `setTimeout(1500)` fallback, cleaned up) warming AppShell+Dashboard+CeremonyGuard — `import()` deduped against the `lazy()` chunks. **Measured (fresh `vite build`):** entry JS 1,536.45 → **283.12 kB** (gzip 438.17 → 87.35, **−81.6%**); entry CSS 268.24 → **67.67 kB** (gzip −74.8%); AppShell chunk 854.21 kB JS + 64.76 kB CSS, ProjectBoard 285.64 kB — **−84% cumulative** from the pre-split 1,747.69 kB. Verified: lint 0 / tests 77/77; `/` DCL 460 ms with zero shell modules; preload fires at 590 ms; shell DOM-identity preserved across client nav (`window.__shell` check); deep-link `/app` clean; Cmd+K + MobileNav intact; screenshots taken. Commit `b8d27e6` on `perf/route-lazy-splitting`, PR [#18](https://github.com/onaxyzogden/Maqasid/pull/18) — **merged to `main` 2026-07-02** (merge `73424af`). ADR: [[2026-07-02-milos-lazy-appshell]]. |
| 2026-07-02 | **App.jsx route pages code-split to `React.lazy`.** A "load mobile preview" request surfaced a blank/slow dev cold-load — `src/App.jsx` eagerly imported ~60 route pages, so Vite dev had to compile 100+ modules before first paint. ~55 page imports converted to `React.lazy(() => import(...))` under the pre-existing top-level `<ChunkErrorBoundary><Suspense fallback={<RouteSpinner/>}>`; only `Landing` + `AppShell` (+ spinner/boundary) stay eager. 5 dead dashboard imports (`FaithDashboard`/`HealthDashboard`/`IntellectDashboard`/`FamilyDashboard`/`EnvironmentDashboard` — never rendered, hidden from ESLint by the PascalCase `varsIgnorePattern`) deleted. `vite.config.js` gained `server.port` PORT-env support (+ `node:process` import for the flat ESLint config) and `.claude/launch.json` `autoPort:true` (preview plumbing, carried together). **Measured (fresh `vite build`):** entry JS 1,747.69 → **1,536.45 kB** (gzip 471.56 → 438.17, −12%); entry CSS 400.10 → **268.24 kB** (gzip 57.52 → 42.56, −33%); ~55 per-route JS+CSS chunks now emitted. Verified: lint 0 (ratchets 0), tests 77/77, and mobile (375×812) render of Landing + lazy Dashboard (`/app`) + lazy Faith pillar (`/app/pillar/faith`) — no ChunkLoadError / error boundary / console errors (DOM/a11y polling; `preview_screenshot` timed out, disclosed). The page-level sibling of [[2026-04-25-milos-chunk-split]]. **Deferred (top next step):** `AppShell` is still eager, so its whole tree (Sidebar/TopBar/MobileNav/IslamicRail+Panel/prayer postures/seed-hydrator/stores) loads on *every* route incl. `/` — the dominant remaining cold-load cost; fix = lazy `AppShell` + move `<Suspense>` into its `<Outlet>`. Also `ProjectBoard`/`AssetsTab`/`ProjectJournal` still eager in App.jsx. **Committed later the same day** as `5001b47` on `perf/route-lazy-splitting`, PR [#18](https://github.com/onaxyzogden/Maqasid/pull/18) — **merged to `main` 2026-07-02** (merge `73424af`); the AppShell deferral was executed in the same PR ([[2026-07-02-milos-lazy-appshell]]). ADR: [[2026-07-02-milos-route-lazy-splitting]]. |
| 2026-07-01 | **LevelNavigator pillar segments made mobile-friendly (app-wide).** The FAITH…COMMUNITY segment strip under "Core Higher Objectives" was unusable on phones — at 375px the 7 columns crammed into ~239px (27px/column), each subseg a 1px-wide tap target, labels truncated to a single letter ("F…" for both Faith and Family). Since `LevelNavigator` is external (`@ogden/ui-components`), the fix is an **app-wide global override** — a MILOS-owned stylesheet [src/styles/level-navigator-responsive.css](src/styles/level-navigator-responsive.css) imported once at [src/main.jsx](src/main.jsx) after the package CSS, anchored on a doubled class `.fln__segments.fln__segments` (specificity 0,2,0; descendants 0,3,0) so it reaches **every** instance regardless of which wrapper renders it: at ≤767px `.fln__segments` stacks to full-width rows (`[label \| bar]`), bar grown to 44px tall so subsegs inherit tappable height + take `rowWidth/N` width; `@media (max-width:479px)` trims the label column. Scope = every strip app-wide — dashboard, all 7 pillar-overview pages, the pillar-submodule pages, the prayer page (via `PrayerLevelNavigator`), and the legacy BBOS stage nav; the interim `.flo`-scoped block was removed as redundant (single source of truth). Desktop ≥768px unchanged; `taskColorFn` + current-tint preserved. Verified live at 375px on `/app` (column stack, bar 44px, subsegs 1px×14px → ~23px×44px, labels de-truncated) via computed-style inspection **and** a screenshot; unchanged at 994px; other surfaces confirmed by mechanism + code parity (behind an unrelated ceremony gate). Committed on `fix/levelnavigator-mobile-stacked-rows` off `main`. ADR: [[2026-07-01-milos-levelnavigator-mobile-stacked-rows]]. Row geometry amended 2026-07-02 into dtc-style cards ([[2026-07-02-milos-levelnav-dtc-card-rows]]). |
| 2026-06-07 | **Backup-modal nag fixed** (commit `e99fcff`). The first-login "Back up your data?" prompt fired far too often. Two causes addressed in [src/store/auth-store.js](src/store/auth-store.js): supabase-js re-emits `SIGNED_IN` on tab refocus and `TOKEN_REFRESHED` on a timer, and each refire re-ran first-login conflict detection — added an in-memory `_authBootUserId` guard so detection runs once per page load per user; and dismissing the prompt did not persist — keeping local without pushing now sets a device-local `sync_backup_dismissed` flag (added to `SYNC_EXCLUDED_KEYS` in [src/services/storage.js](src/services/storage.js) so it never rides the cloud snapshot) which suppresses the `has_local_no_cloud` branch. Manual backup via Settings → "Sync now" unaffected. |
| 2026-04-11 | Technical audit remediation Sprint 4: threshold-store sessionStorage migration for ceremony state (#19), Niyyah skip action (#14), geolocation 3-attempt retry with IP fallback (#20). |
| 2026-04-11 | Sprint 5: localStorage guardrails — 500 KB attachment limit with quota estimation (#22), message pruning at 500/channel (#23), dual-contact-stores ADR (#25). |
| 2026-04-11 | Sprint 6: MoneyDashboard rewritten from mock data to store-computed values (#15). Dashboard empty state for new users (#11). TechOverview "Simulated" badge (#16). SearchPalette expanded to 5 sources (#18). BudgetTab placeholder (#24). |
| 2026-04-11 | Sprint 7: BBOS Two-Factory visual split + Assembly Gate UI (#32A-B). G-Label descriptions in picker (#32C). Pipeline sub-stage progress indicators (#32D). Mobile breadcrumb visibility (#33). |
| 2026-04-11 | Sprint 8: Documentation freshness — 4 CONTEXT.md files updated, wiki entities synced, lessons learned populated, bundle size ADR filed. |
| 2026-04-12 | AyahBanner polish: Amiri font, centered layout (label + Arabic + translation + source), col-edge overlap fix (`left: var(--edge-w)`), topbar z-index 10→16, opaque surface background. Spacing tweak: `--space-6` → `--space-3` below banner. Intellect submodule renamed "Skill Proficiency" (was "Professional Mastery") across 5 files. Family Islamic data bootstrapped in `MODULE_ATTRS` — dua Quran 25:74, Al-Wadud + Al-Qayyum, full readiness + reflection. |
| 2026-04-12 | Dashboard refresh: replaced static PillarCard grid with `PillarProgressStrip` (task-based status coloring, pillar accent borders, Arabic roots), replaced "coming soon" with live Maqasid Focus panel (pillars by open task count), trimmed stat cards to 2, renamed EPH → "Today", added OVERVIEW + MAQASID AL-SHARI'AH section labels. Bug fixed: pillar resolution must use `project.id.startsWith(pillar.id + '_')` as primary (subModuleId strings do not match project.moduleId for faith/life/intellect/family/environment). |
| 2026-04-11 | Family module: added Family Office — embedded Office module accessible as (1) tab within `/app/family` (FamilyPage) and (2) dedicated sidebar entry at `/app/family-office`. |
| 2026-04-09 | Wiki entity page bootstrapped. Monorepo expanded with ogden-hub subtree and atlas submodule. Knowledge graph at 882 nodes / 1,438 edges / 47 communities. |
| 2026-04-17 | Subtask Sources view upgraded with `QuranEmbed` (live quran.com iframe, offline fallback) and `HadithCard` (509 bundled hadith, Amiri Arabic). Seed copy standardized (Why?/How?, heading renames, HR removed). 4 module icons refreshed; lucide-react bumped 0.511 → 1.8. Commits `1ae2793`, `cd5bd55`. |
| 2026-04-18 | Universal grounding plan Phase 0 complete. Schema: `docs/grounding-schema.md` (two-axis: `provenanceTier` + `relevance`; Niyyah-blocks rule; legacy-string migration path). Linter: `scripts/lint-grounding.mjs` — 1,829 subtasks across 7 pillars, all legacy-string, zero missing. Runtime prompt: `docs/grounding-runtime-prompt.md` — dual-corpus (Muslim Scholar `1c17b03b` citations; Claude Scholar `91d2bd2b` query-craft), pillar-boundary session atoms, §9 Session Continuity Block. ADR filed: `wiki/decisions/2026-04-18-milos-grounding-two-axis.md`. Both corpora registered in CLAUDE.md registry and `wiki/references/notebooklm-grounding-corpora.md`. |
| 2026-04-21 | Maqasid Comparison Wheel + Path to Excellence promoted from the prototype route onto the real Faith overview page, positioned below the Level Navigator at all three levels. Shared `LevelOverviewPage` gained opt-in props (`showComparisonWheel`, `ComparisonWheelComponent`, `wheelCenterLabel`, `ExcellenceCardsComponent`) — Faith opts in, other pillars untouched. Wheel binds to the existing `useModulesProgress` selector (same source as bento `MasteryRing`, so the two stay in lockstep). New `levelColor` prop recolors the entire wheel — radial progress gradient, outer label band, and segment strokes — via opacity-falloff stops + a `--mcw-level-color` CSS var. Components moved to `src/components/faith/`; prototype route/page/sidebar entry deleted. ADR: `2026-04-21-faith-dashboard-wheel-promotion.md` (supersedes the 2026-04-20 prototype ADR). |
| 2026-04-20 | Dashboard Wheel Test prototype added at `/app/dashboard-wheel-test` (sidebar FlaskConical entry). Custom SVG `MaqasidComparisonWheel` (hub + 5 annular progress sectors + outer label ring with curved `<textPath>`) and `PathToExcellenceCards`. Center-origin gradient via `gradientUnits="userSpaceOnUse"` on shared `radialGradient`. Current Dashboard untouched; promotion pending approval. ADR: `2026-04-20-dashboard-wheel-test-prototype.md`. Commit `df3924b`. |
| 2026-04-19 | Dashboard Sanctuary Mode shipped — six-phase revamp reshaping the dashboard around the authored Niyyah. Task schema gained `pillarId`/`submoduleId` with idempotent `backfillPillarFields()` migration (`bbiz_task_pillar_migrated_v1` sentinel) + `getFocusTasks()` selector. `--dashboard-accent` CSS variable on `.insight` root drives Sun & Stars treatment on TodayFocusSection + PillarProgressStrip. New components: `FocusTaskList` (Today's Deep Work + collapsible Keep the Lights On), `RippleRing` (SVG progress ring on Sun card + debounced "one step closer" toast), `ContextWidgetSlot` registry with four widgets (`PrayerCountdownWidget` via extracted `useNextPrayer` hook, `PomodoroWidget`, `HydrateWidget`, `PriorityProjectWidget`), `EveningReflectButton` + `EveningReflectModal` (ḥāl al-qalb evening grid; reflection archived into `niyyahHistory`). Day-rollover self-healing: new `rolloverIfStale` action in `threshold-store` wired to Dashboard mount — archives stale niyyah (with nested reflection) into history and clears current-day slots regardless of entry path. Yesterday's Echo in morning Niyyah modal surfaces yesterday's submodule + morning feeling + evening reflection with a "Continue the journey" prefill button. ADR: `2026-04-19-dashboard-sanctuary-mode.md`. |
| 2026-04-21 | PropheticPath prophetic-sunnah phase tasks. `buildTasksForNode(nodeId, projects, tbp, { phase })` gained phase filter (`before`/`main`/`after`) via `phaseMatchers` on each prayer node in `prophetic-path-submodules.js`. Four new parent tasks authored in `faith-seed-tasks.js` (pre-prayer sunnah, post-prayer adhkar, Ishraq, prophetic supplications) with full Amanah-Gate source blocks from NotebookLM Muslim Scholar `1c17b03b` (≈30 sahih citations). PropheticPath.jsx Before button rewired from Threshold modal to phase-filtered task list matching the After pattern. `backfillAndStripSeeds()` in `project-store.js` extended to append new seed tasks by title-diff so existing boards inherit them. Raw notebook answer preserved at `tasks/notebook-prophet-prayers-answer.md`. Verified 18/18 prayer-node × phase buckets populate. ADR: `2026-04-21-prophetic-prayer-phase-tasks.md`. |
| 2026-04-17 | Ummah pillar converted to PillarLevelPage pattern. Added `UMMAH_BOARDS` (18 boards: 6 submodules × 3 levels), `ensureUmmahProjects`, `UmmahLevelNavigator`, `UmmahPillarPage`. All 6 submodule pages (Collective, Neighbors, Community, MTC Land/Seasonal/Residency) rewritten from legacy `PillarHeader + ViewToggle + OverviewCards/MaqasidTable` to minimal `<UmmahPillarPage pillarKey="…" />` wrappers. OVERVIEW + MAQASID content relocated to `UmmahDashboard` as collapsible "Frameworks" section. Build passes at 2645 modules. Seeded tasks (450 subtasks across 90 tasks) authored in prior session via 6 parallel agents — citation verification pass still deferred. |
| 2026-04-22 | Wheel Round 5.5: follow-up pass on the Round 5 wheel. **(1)** Mithaq paused on Faith overview — `mithaqDomain` prop removed from `FaithLevelOverview`; store/hook/dormant-state infrastructure retained for future re-enable. **(2)** Hover contrast inversion — aura opacity 0.75→0.5, icon flips to dark `#0c1a20` (Lucide currentColor) instead of carrying a bright drop-shadow; eliminates Chrome foreignObject bounding-box square. **(3)** Wheel becomes navigation surface — both outer `.mcw-band` and inner sector carry `onClick` + Enter/Space, `role="button"`, `tabIndex={0}`, `useNavigate(seg.route)`; `LevelOverviewPage` now passes `p.route` through to segments; pillars without a route silently no-op. **(4)** Cursor-handoff crossfade — 90ms `setTimeout`-based leave-debounce keeps hover state continuous across sectors; CSS transitions moved from inside `:has()` rules to base selectors so opacity tweens both directions (prior rule only animated entry to the dim set). Duration 240→360ms. **(5)** iOS Safari icon-position fix — `<foreignObject>` + `<div>` replaced with nested `<g transform="translate"><g class="mcw-icon-wrap"><Icon/></g></g>`; WebKit's foreignObject mispositioning with CSS custom properties + fractional `polar()` x/y is sidestepped. `.mcw-icon-wrap` CSS adapted for SVG geometry (`transform-origin: 9px 9px; transform-box: fill-box`). ADR: `2026-04-22-wheel-clickable-submodules-ios-fix.md`. |
| 2026-04-25 | Marketing-site MILOS landing hero (`website/milos/index.html`) realigned to the OLOS / MTC family pattern. Removed standalone `.hero-symbol` element (lozenge folded into eyebrow). Eyebrow rewritten as `◊ · MILOS · Islamic Life Operating System · In Development`. Headline replaced negation-correction (_"Your life is not seven separate problems. It is one trust — with seven dimensions."_) with forward statement: _"A way to organize your whole life — as the trust it always was."_ Subhead updated — "ummah" → "community", "Maqasid al-Shariah" → "higher objectives of Islamic law"; double-negation removed; Al-Hashr 59:18 image ("what your life is putting forth for tomorrow") lifted into body copy. Ayah unchanged. No CSS or app code touched. |
| 2026-04-25 | MILOS marketing eyebrow glyph corrected: `&loz;` (U+25CA ◊ filled lozenge) → `&#9671;` (U+25C7 ◇ open lozenge). Aligns the product-page eyebrow with the existing home-card glyph; the open form pairs better with OLOS's open ○ in the family treatment. Canonical: `◇` for MILOS across all surfaces. |
| 2026-04-25 | Pre-test audit Phase A — Tier-1 blockers resolved before next live click-through. `pillar/faith…environment` thick routes wrapped in `<CeremonyGuard moduleId="{name}-core" isLevel1>`; `pillar/ummah` wrapped with `moduleId="ummah"`; `pillar/:pillarId` catch-all wrapped in `<CeremonyGuardDynamic paramKey="pillarId">`; `pillar/moontrance` deferred pending Phase B authoring of top-level `MODULE_ATTRS['moontrance']`. Storage/migration silent catches now log via `console.warn('[bbiz]…')` and dispatch `bbiz:storage-error` events on write/backup/restore failures (read paths warn-only, fallback preserved). Onboarding `finish()` seeds today's niyyah from selections or marks day skipped. Audit findings T1.1 (Moontrance MODULE_ATTRS) + T1.4 (toastStore orphan) verified incorrect on inspection — corrections recorded; cross-referencing docstrings added to both toast stores. ADR: [[2026-04-25-milos-pre-test-tier-1-fixes]]. Build 2764 modules, exit 0. |
| 2026-04-25 | Pre-test audit Phase C — Tier-3 cleanup, session close. **(C.4)** Persistence-tier comment block added to `threshold-store.js` documenting the three storage tiers (PERSISTENT localStorage `thr_*`, SESSION sessionStorage `thr_opening_mid`/`thr_closing_mid`, EPHEMERAL in-memory). **(C.3)** Created `src/lib/format-date.js` canonical helper (`formatDate(input, variant?, fallback?)` with 5 variants, default `MMM d, yyyy`); filed `wiki/concepts/date-format-canonical.md`; migrated the one `date-fns format()` site in CompanyInfoTab. Audit's "50 sites" was inflated — conflated `format()` (1) with locale-aware `toLocaleDateString` (~30, intentionally untouched). **(C.2)** Migrated 6 hard-coded hex sites to tokens: PillarBoard.css 4 sites (`#4ab8a8` → `var(--primary)`, `#3da396` → `var(--primary-hover)`, `#8b5cf6` → `var(--mod-people)`); TaskDetailPanel.css 2 sites (`#2563eb` → `var(--col-progress)`, `#8b5cf630` → `color-mix(... var(--mod-people) 19% ...)`). **Shimmer-keyframe consolidation deferred — audit correction:** the 4 keyframes animate different properties (SVG `stroke-opacity` pulse, `transform translateX` sweep, two `background-position` gradient sweeps) and aren't redundant. **(C.1) Audit correction — dark mode coverage was already correct.** The audit counted explicit `[data-theme="dark"]` selectors (5 files) but missed the token-driven mechanism: 100+ tokens remap under `[data-theme="dark"]` in `tokens.css:252-393`. Verified live in dark-mode preview — body, TopBar, Sidebar, Dashboard all flip correctly. The genuine gaps were the hard-coded hex sites (closed in C.2). ADR: [[2026-04-25-milos-tier-3-cleanup]]. Pre-test audit fully closed; live click-through test unblocked. Build 2765 modules, exit 0. |
| 2026-04-25 | Pre-test audit Phase B — Tier-2 polish. (B.1) Replaced 23 `stub()` entries in `pillar-wisdom.js` + `pillar-next-actions.js` with curated Quranic-grounded content across Life/Intellect/Family/Wealth/Environment/Ummah; all Arabic + English fetched via quran.ai MCP (Amanah Gate honored). Moontrance + Ogden stubs preserved. (B.2 audit correction) Ummah seed-tasks already grounded: lint reports 0 missing across 525 subtasks (legacy-string Markdown); the 2026-04-17 deferral was the two-axis migration, not citation authoring. (B.3) Prophetic Path graduated `/app/prophetic-path-test` → `/app/prophetic-path` with `<Navigate replace>` redirect, "(prototype)" tooltip dropped from Sidebar; MobileNav, TodayFocusSection, TimelineIslamicContent, IslamicPanel link/active-detection updated. (B.4) New `ChunkErrorBoundary` (class, `getDerivedStateFromError` + Retry → `window.location.reload()`) wraps App.jsx global `<Suspense fallback={<RouteSpinner />}>` and TaskDetailPanel SubtaskSources `<Suspense fallback={<SourcesSkeleton />}>`. PropheticPath now consumes `usePrayerTimes` `loading + error + requestLocation` and renders a degraded alert when `!timings && !loading`. (B.5) `safeSet` adds `navigator.storage.estimate()` fire-and-forget probe for writes >200 KB (throttle 30s, dispatches `bbiz:storage-error` at >90% usage). Moontrance Land/Seasonal/Residency converted to `lazy(() => import(...))`; index chunk 2,015 → 1,694 KB (~322 KB savings). **LevelNavigator chunk still 4.7 MB raw** — the 3 Moontrance pages weren't the source; root cause is shared data hoisting across all 8 pillars and needs Vite manual-chunks investigation, deferred. ADR: [[2026-04-25-milos-tier-2-polish]]. Build 2765 modules, exit 0. |
| 2026-04-29 | Three recurring Windows/preview/layout root-causes resolved together. `.claude/launch.json` pinned to `npm.cmd` so the preview UI button (which spawns without a shell) works on Windows. UA sniff for `Claude/` in `src/main.jsx` adds a `.reduce-motion` class on `<html>`; new global rule in `src/styles/global.css` collapses every `*`'s animation/transition durations to ~0 ms, fixing the `preview_screenshot` 30 s hang caused by `.hero-marquee-track`'s infinite animation. New `--main-balance-end` CSS var on `.app-shell` (set in `AppShell.jsx`) mirrors `sidebarPx + edgePx` as `padding-inline-end` on `.app-main` whenever the right Islamic panel is closed, centering the content column in the viewport; `.insight` gets `width: 100%; margin-inline: auto;` (preserving its 1200 px max-width). All three `.dash-tier--*::before` gradient panels collapsed to `.dash-tier::before { content: none; }` — tier identity (Qalb · نية / Amal · عمل / Barakah · بركة) now conveyed by eyebrow tint alone, dashboard reads as one continuous surface. ADR: [[2026-04-29-milos-preview-and-dashboard-centering]]. |
| 2026-06-05 | Islamic Layer icon rail shipped. New always-visible 64px vertical rail pinned to the far-right grid column (desktop only), mirroring the collapsed left sidebar — restores left/right shell symmetry. New `useIslamicSections.js` hook is the single source of truth for the panel's ordered sections + availability, consumed by both rail and panel so they never drift. New non-persisted store state `islamicActiveSection`/`islamicSectionNonce` + `focusIslamicSection(id)` (opens panel, targets section, bumps nonce so re-clicks re-scroll). `AppShell` grid grew 5→6 columns; `--main-balance-end` now pads by `max(0, leftChrome − railPx)`. `ILSection` force-opens + `scrollIntoView` when targeted; prayer/citations get `.il-anchor` wrappers scrolled by a panel-level effect (citations auto-revealed). Build/lint/test green (70 tests); preview verified. Also this session: `deploy.yml` build step now injects `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` from GitHub Actions secrets (secrets must be added manually by the operator). ADR: [[2026-06-05-milos-islamic-layer-rail]]. |
| 2026-06-14 | Right Islamic sidebar made to mirror the left -- app shell collapsed from 6 to 5 symmetric grid columns `[sidebar] [left-edge] [main] [right-edge] [right-sidebar]`. The permanent 64px `IslamicRail` column (old col 6) is gone; the single right column now renders the rail when collapsed and `IslamicPanel` when expanded, and an always-present right `col-edge` chevron mirrors the left (drag-resize + click-toggle). Panel header X becomes `ChevronRight`; `--main-balance-end` simplified to `max(0, sidebarPx - railPx)`; no store changes (the [[2026-06-05-milos-islamic-layer-rail]] section-nav machinery is retained). Build/lint/test green (77 tests); preview DOM verified. On `main` via PR #12 (merged 2026-06-14); redundantly re-merged via PR #14 (cherry-pick `287d529`, merged 2026-07-02) -- the interim "orphaned" read was a stale-fetch artifact. ADR: [[2026-06-14-milos-right-sidebar-symmetry]]. |
| 2026-04-22 | Maqasid Comparison Wheel Round 5: Nur Aura + Icon Bloom + Mithaq Activation ritual. Each pillar icon now sits in a `.mcw-pillar-vessel` with a radial-gradient aura (new `brightAura` OKLCH field at L=0.78) — hover blooms icon scale 1.22× + aura 1.6× with level-color glow; 100% complete state gets persistent lit aura. New `useMithaqStore` (Zustand + `persist`) + `useMithaqHold` hook gate the Faith wheel behind a daily 1.5s press-and-hold on the Qalb hub. When dormant: sectors desaturated, needle/next-action-card/wisdom-tooltip suppressed, hub shows `Press & hold to renew` Amiri hint. Shimmer ring draws clockwise around hub via `pathLength=1` + `strokeDashoffset={1 - progress}`. On release-early: RAF-driven ease-in drain (280ms). On completion: 80ms silence → staggered aura ignition (50ms/index) + `aria-live="polite"` announces "FAITH covenant renewed." Daily reset at next 5am local (Fajr proxy). Keyboard: Space/Enter press-and-hold, Escape aborts. `prefers-reduced-motion` disables all bloom/ignition. `mithaqDomain="faith"` prop; other pillars unaffected. Build 2706 modules, lint clean. ADR: `2026-04-22-mithaq-activation-nur-aura.md`. |
| 2026-07-28 | Landing page rebuilt against a pasted UI/UX briefing (scope: `Landing.jsx` + `landing.css` only; `tokens.css`/`global.css` untouched). Structural variety — evidence split into an asymmetric bento (provenance) plus a borderless straight-line grid (relevance), How It Works into hairline columns with `.motif-ghost-text` numerals, Orientation into an overlapping rotated card deck, and a `.section-head--split` variant alternating with the centred one. Seven of the briefing's eleven micro-animations shipped; four skipped as having no home on a marketing page. **The page had no mobile navigation at all below 767px** — `.landing-nav-links { display: none }` with no replacement; built a top-anchored sheet (icons per link, Escape/backdrop/link close, body-scroll lock, focus trap + return, `inert` when closed) deliberately *not* bottom-pinned. Also fixed anchor-jump offsets (`scroll-margin-top` on all six sections, offset against the taller nav state). `useScrollProgress` + `useFirstReveal` extracted from `PropheticPathPreview` to `src/components/landing/use-landing-scroll.js`; `useFirstReveal` hardened with a timer fallback for environments that never deliver IntersectionObserver callbacks, which also un-stranded `.ppp-phone`. Two reduced-motion guards had to clear `transition` outright, not shorten it — `.reduce-motion` clamps `transition-duration` but **not** `transition-delay`. Sunnah Mode exempt from restructure (quote strings are character-locked). `d5d4a37`, `46efe0b`, `68d07b0`. |
| 2026-07-28 | **Landing copy pass — "sounds robotic" diagnosed as one stock phrase in five headline slots, plus two live false claims.** The page is *not* uniformly robotic; most body copy has a real voice ("Four ways out, none of them guilt", "the day turns over at Maghrib, not midnight") and none of it changed. The defect was countable: `"every dimension of … life"` ×5, `"purpose/purposeful"` ×3, `"truly matter(s)"` ×2, `"align"` ×2, all in headline positions — reduced to 5→1 (the locked FAQ), 3→0, 2→0, 2→0 and verified by grep. Rewrote the joints, not the sections: CTA block (dropped a crowd appeal implying a userbase the page cannot evidence; `Get Started Free` → `Choose your path`, which names its real destination), How It Works title, the three Title Case step titles on an otherwise sentence-case page, and the footer bottom line (deleted rather than replaced — `.footer-bottom` is `space-between`, so the lone copyright falls to `flex-start` with no CSS change). Hero h1 `"what truly matters"` → `"Seven objectives. One next step."`; tiered as explicitly droppable given [[feedback_bbos_subhead_protected]], approved in full, and Orientation retitled to resolve the resulting phrase collision. **Separately, a second stale-truth bug of the [[2026-07-27-milos-disable-online-accounts]] class:** the hero marquee and How It Works subtitle both still promised cross-device sync while `CLOUD_ACCOUNTS_ENABLED = false` and the FAQ on the same page said sync was off — a self-contradicting privacy claim, committed separately so it is cherry-pickable. All seven FAQ answers, every `DAY_VARIANTS[].quote`, and the evidence disclaimer classified as locked before any edit; `AMANAH_TIERS`/`RELEVANCE_CHIPS` ruled out of scope rather than edited in passing. `85e29ea`, `59e3d06` — [PR #35](https://github.com/onaxyzogden/Maqasid/pull/35), which also carries the four structural commits above (PR #34 had merged before they landed). |
